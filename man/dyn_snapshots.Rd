% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mixing.R
\name{dyn_snapshots}
\alias{dyn_snapshots}
\title{The network sliced into snapshots}
\usage{
dyn_snapshots(
  dn,
  at = NULL,
  sessions = c("bounded", "collapse", "separate"),
  sample = c("window", "instant")
)
}
\arguments{
\item{dn}{A temporal network from \code{\link[=dynet]{dynet()}}.}

\item{at}{Optional numeric time. When given, only the bin containing that
time is returned.}

\item{sessions}{How to treat sessions, as in \code{\link[=dyn_centrality]{dyn_centrality()}}.}

\item{sample}{\code{"window"} or \code{"instant"}, as in \code{\link[=dyn_centrality]{dyn_centrality()}}.}
}
\value{
A \code{data.frame} with one row per active edge per bin: \code{session}
(when present), \code{time}, \code{from}, \code{to}, \code{weight} and \code{n_spells}. A pair
joined by more than one spell in the same bin is one edge, with
\code{n_spells} recording how many spells were collapsed -- so the edge counts
here agree with those from \code{\link[=dyn_metrics]{dyn_metrics()}}.
}
\description{
The edges alive in each time bin, as one tidy table. Useful for exporting a
slice, for feeding a layout routine, or for checking by eye what the metric
verbs are seeing.
}
\examples{
dn <- dynet(school_contacts)
dyn_snapshots(dn, at = 3)

}
