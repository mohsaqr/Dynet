% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/communities.R
\name{.genlouvain_block}
\alias{.genlouvain_block}
\title{One generalized-Louvain optimisation of a single projection block}
\usage{
.genlouvain_block(
  layers,
  gamma,
  omega,
  coupling,
  visit,
  tol = 1e-10,
  max_passes = 20L
)
}
\arguments{
\item{layers}{A list of \code{T} symmetric \verb{n x n} matrices.}

\item{gamma}{Resolution.}

\item{omega}{Interlayer coupling.}

\item{coupling}{\code{"ordinal"} or \code{"categorical"}.}

\item{visit}{Integer permutation of the states, giving the order phase one
considers them in. This is the only place randomness enters.}

\item{tol}{Minimum improvement that counts as a move.}

\item{max_passes}{Cap on aggregation passes.}
}
\value{
A list with \code{membership} (integer, one label per state, in
slice-major order) and \code{passes} (the number of passes used).
}
\description{
Phase one moves individual states; phase two and later move whole
communities on the aggregated modularity matrix. The \verb{nT x nT} modularity
matrix is never formed: phase one reads slice-local adjacency, strength and
community-strength, and the aggregate is built only over the communities
phase one produced, which number in the tens.
}
\details{
Aggregating the \strong{modularity} matrix rather than the adjacency matrix is
the step a naive implementation gets wrong. The multislice null is
slice-local, so it is not preserved by summing adjacency; once the null is
inside the aggregate, later passes optimise the bare sum with no further
null subtraction.
}
\keyword{internal}
