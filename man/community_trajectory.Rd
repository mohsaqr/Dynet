% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/communities.R
\name{community_trajectory}
\alias{community_trajectory}
\title{What each vertex did across the community structure}
\usage{
community_trajectory(
  x,
  measure = c("flexibility", "promiscuity", "persistence"),
  reference = NULL
)
}
\arguments{
\item{x}{A \code{dynet_communities} frame whose labels are consistent across
bins -- either from \code{\link[=temporal_communities]{temporal_communities()}} with \code{omega > 0}, or from
\code{\link[=match_communities]{match_communities()}}.}

\item{measure}{One or more of \code{"flexibility"}, \code{"promiscuity"},
\code{"persistence"}, \code{"recruitment"} and \code{"integration"}.}

\item{reference}{For \code{"recruitment"} and \code{"integration"}: the name of a
vertex attribute on the network the partition came from, or one group
label per vertex. Those two measures compare a vertex's allegiance to its
own reference group against its allegiance to the others, so they have no
meaning without one.}
}
\value{
A \code{dynet_metric} at node level, one row per vertex per measure,
with columns \code{session} (only when the network has sessions), \code{node},
\code{measure} and \code{value}. Every measure here summarises the whole series, so
there is no \code{time} column; the per-bin and whole-series forms of
persistence, and the pairwise allegiance table, are reached by
\code{as.data.frame(x, what = )}.
}
\description{
Once labels mean the same thing in every bin, the interesting quantities
are about vertices rather than communities: how often one changes group,
how many groups it has belonged to, how reliably it stays put, how often
two vertices are found together, and how tightly a vertex sticks to its own
reference group rather than visiting others.
}
\details{
Let \eqn{C_{it}} be the community of vertex \eqn{i} in bin \eqn{t}, over
\eqn{T} bins.

\strong{Flexibility} is the share of consecutive bins in which a vertex changed
community, \eqn{\frac{1}{T-1}\sum_{t=2}^{T}[C_{it} \ne C_{i,t-1}]}.

\strong{Promiscuity} is how much of the whole community structure a vertex
visited: its distinct-label count minus one, over the \strong{global}
distinct-label count minus one. A vertex that never moves scores zero; one
that visits every community scores one. The denominator is global, not per
vertex; that is teneto's convention and it is what makes the measure
comparable across vertices.

\strong{Persistence} is the complement of flexibility, and comes at three
granularities: per vertex, per bin, and one number for the whole series.
The per-bin form is \code{NA} in the first bin, which has no predecessor.

\strong{Allegiance} \eqn{P_{ij} = \frac{1}{T}\sum_t [C_{it} = C_{jt}]} is how
often two vertices were in the same community. It divides by the number of
bins, not by the bins in which both were active -- teneto's convention,
replicated here so the numbers agree.

\strong{Recruitment} is a vertex's mean allegiance to the other members of its
own \code{reference} group; \strong{integration} is its mean allegiance to vertices
outside it.

\emph{Why the labels must be matched first.} All of these read a change of label
as a change of group. If the labels are per-bin arbitrary -- which they are
whenever the slices were solved independently -- then flexibility measures
relabelling noise and nothing else. This verb therefore refuses to run on
an unmatched partition rather than returning a number that looks fine.

\emph{Inactive vertices.} A vertex inactive in a bin still carries the label its
identity arc brought it, matching \code{\link[=projection]{projection()}}'s waiting convention, so
it is neither dropped nor treated as having left. \code{n_inactive_states}
records how many states that covers, so the reader can judge.
}
\examples{
dn <- dynet(school_contacts)
found <- temporal_communities(dn, step = 5, window = 5, seeds = 1:5)
community_trajectory(found)
as.data.frame(community_trajectory(found), what = "time")

}
\references{
Bassett, D. S., Wymbs, N. F., Porter, M. A., Mucha, P. J., Carlson, J. M.,
& Grafton, S. T. (2011). Dynamic reconfiguration of human brain networks
during learning. \emph{PNAS}, 108(18), 7641-7646.

Papadopoulos, L., Puckett, J. G., Daniels, K. E., & Bassett, D. S. (2016).
Evolution of network architecture in a granular material under compression.
\emph{Physical Review E}, 94(3), 032908.

Bassett, D. S., Porter, M. A., Wymbs, N. F., Grafton, S. T., Carlson, J.
M., & Mucha, P. J. (2013). Robust detection of dynamic community structure
in networks. \emph{Chaos}, 23(1), 013142.

Bassett, D. S., Yang, M., Wymbs, N. F., & Grafton, S. T. (2015).
Learning-induced autonomy of sensorimotor systems. \emph{Nature Neuroscience},
18(5), 744-751.

Thompson, W. H., Brantefors, P., & Fransson, P. (2017). From static to
temporal network theory. \emph{Network Neuroscience}, 1(2), 69-99.
}
