% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mixing.R
\name{.mixing_counts}
\alias{.mixing_counts}
\title{Count active binary dyads by group pair}
\usage{
.mixing_counts(a, group, pairs, directed)
}
\arguments{
\item{a}{Numeric vertex adjacency matrix for one bin.}

\item{group}{Integer group index for each matrix vertex.}

\item{pairs}{Data frame of integer \code{from} and \code{to} group indices.}

\item{directed}{Whether group pairs are ordered.}
}
\value{
Numeric vector of active binary-dyad counts, one per \code{pairs} row.
}
\description{
Unlike path kernels, this reducer preserves a present diagonal as one
explicitly retained loop. An undirected within-group block contains each
ordinary edge twice and each loop once, so only its off-diagonal sum is
halved.
}
\examples{
a <- matrix(c(1, 0, 2, 0), 2, 2)
Dynet:::.mixing_counts(a, c(1L, 2L), expand.grid(from = 1:2, to = 1:2), TRUE)
}
\keyword{internal}
