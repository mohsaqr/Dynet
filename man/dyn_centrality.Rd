% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/centrality.R
\name{dyn_centrality}
\alias{dyn_centrality}
\title{Deprecated name for \code{centrality_series()} and \code{path_centrality()}}
\usage{
dyn_centrality(
  dn,
  measure = "degree",
  scope = c("snapshot", "temporal"),
  sessions = c("bounded", "collapse", "separate"),
  sample = NULL,
  damping = 0.85,
  mode = c("all", "out", "in"),
  start = NULL,
  end = NULL,
  step = NULL,
  window = NULL,
  exponent = 1,
  traversal_time = 0,
  prestige = "indegree",
  rescale = FALSE,
  lambda = 1,
  plot = FALSE
)
}
\arguments{
\item{dn, measure, sessions, sample, damping, mode, start, end, step, window, exponent, prestige, rescale, lambda, plot}{As in \code{\link[=centrality_series]{centrality_series()}}.}

\item{scope}{\code{"snapshot"} (the default) or \code{"temporal"}.}

\item{traversal_time}{As in \code{\link[=path_centrality]{path_centrality()}}; nonzero only with
\code{scope = "temporal"}.}
}
\value{
A node-level \code{dynet_metric}, as returned by the function it
forwards to.
}
\description{
\code{dyn_centrality()} was split in two. Its default \code{scope = "snapshot"} is
now \code{\link[=centrality_series]{centrality_series()}}; \code{scope = "temporal"} is \code{\link[=path_centrality]{path_centrality()}}
for closeness and betweenness and \code{\link[=reachability]{reachability()}} for reach. The old
name still works and returns what it always returned, with a warning of
class \code{dynet_deprecated}. It will be removed in a future release.
}
\section{Conditions}{

Warning: \code{dynet_deprecated} on every call. Errors are those of the
function it forwards to, plus \code{dynet_bad_input} for \code{mode}, \code{step} or
\code{window} with \code{scope = "temporal"} and a nonzero \code{traversal_time} with
\code{scope = "snapshot"}.
}

\examples{
dn <- dynet(school_contacts)
# Warns, then returns what centrality_series(dn) returns.
dyn_centrality(dn)
}
\keyword{internal}
