% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/motifs.R
\name{motifs}
\alias{motifs}
\title{Delta-temporal three-node motif census}
\usage{
motifs(
  dn,
  delta,
  output = c("global", "local"),
  sessions = c("bounded", "collapse", "separate"),
  start = NULL,
  end = NULL
)
}
\arguments{
\item{dn}{A directed temporal network from \code{\link[=dynet]{dynet()}}.}

\item{delta}{Window width, in the network's time unit. A triple of events
is a motif instance when its last event is no later than \code{delta} after
its first. \strong{Required, with no default}: there is no principled default
width, and a silent one would make every count an artefact of a number
the user never chose. \code{\link[=gaps]{gaps()}} is the honest way to pick one.}

\item{output}{\code{"global"}, the default, for the 40-row network census, or
\code{"local"} for 40 rows per vertex.}

\item{sessions}{How to treat sessions, as in \code{\link[=pshifts]{pshifts()}}.}

\item{start, end}{Optional inclusive query limits.}
}
\value{
A \code{dynet_motifs} data frame. Under \code{output = "global"} the columns
are \code{motif} (the 1-based raphtory index), \code{family}, \code{pattern} and
\code{count}; under \code{"local"} a \code{node} column precedes them. A leading
\code{session} column is present under \code{sessions = "separate"}. All 40 classes
are always present, zeros included -- a census with rows silently missing
is not a census. Print it, \code{\link[=summary]{summary()}} it, \code{\link[=plot]{plot()}} it, or take the plain
frame with \code{\link[=as.data.frame]{as.data.frame()}}.
}
\description{
Counts the 40 Paranjape three-edge, three-node temporal motif classes, in
the class ordering of raphtory 0.17.0 so a census can be checked against an
independent implementation index by index.
}
\details{
\strong{The definition.} Three incompatible motif families exist in the
literature. This adopts Paranjape, Benson & Leskovec (2017): exactly three
edge events on at most three distinct vertices, with
\code{t_last - t_first <= delta} over the three times in sorted order. Kovanen et
al. (2011) instead allow arbitrary size and bound \emph{consecutive} gaps, which
is a strictly weaker condition and agrees only for two-edge patterns; that
family's instance count is unbounded and does not reduce to a fixed census.

The three edges are three distinct \emph{events}; the same pair may repeat,
which is exactly what the two-node classes are. Loops are excluded, and
instances may share edges -- every qualifying triple is counted, as in the
reference implementations. Edge-disjoint counting is a different and much
harder problem, and is not what any reference computes.

\strong{Local counting is not uniform across families}, and this is raphtory's
rule, adopted verbatim. For a star only the centre counts the instance; for
a triangle all three vertices do; for a two-node motif the global census
itself already records one count at each endpoint's class. So the local
counts sum to the global counts for stars and two-node motifs, and to three
times the global counts for triangles.

\strong{Simultaneous events} are ordered by the deterministic key
\verb{(time, from, to, spell)}. Paranjape assumes distinct timestamps; this is
Dynet's choice, so that a census never depends on input row order.

The enumeration is the direct one, quadratic in the number of events
sharing a window. Paranjape's incremental algorithm is linear; this is not
it, and a very large \code{delta} on a bursty network will be slow.
}
\section{Conditions}{

Errors: \code{dynet_needs_directed} on an undirected network, since the 40-class
taxonomy is defined by edge directions and has no honest undirected
reading; \code{dynet_bad_input} when \code{delta} is missing, not a single finite
non-negative number, or when \code{dn} is not a \code{dynet}; \code{dynet_no_sessions}
under \code{sessions = "separate"} without a session column;
\code{dynet_count_overflow} when a class exceeds \code{2^53}.
}

\examples{
dn <- dynet(school_contacts)
census <- motifs(dn, delta = 2)
census

}
\references{
Paranjape, A., Benson, A. R., & Leskovec, J. (2017). Motifs in temporal
networks. \emph{WSDM '17}, 601-610. \doi{10.1145/3018661.3018731}

Kovanen, L., Karsai, M., Kaski, K., Kertesz, J., & Saramaki, J. (2011).
Temporal motifs in time-dependent networks. \emph{Journal of Statistical
Mechanics}, P11005. \doi{10.1088/1742-5468/2011/11/P11005}
}
\seealso{
\code{\link[=gaps]{gaps()}} for choosing \code{delta}, and \code{\link[=pshifts]{pshifts()}} for the dyadic
turn-taking census, which counts the same events with a different arity.
}
