% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paths.R
\name{.latest_departure_search}
\alias{.latest_departure_search}
\title{Latest-departure journeys from one source into every target}
\usage{
.latest_departure_search(
  dn,
  enc,
  source,
  lower,
  upper,
  bounded,
  traversal_time = 0,
  activity_mode = c("collapse", "separate"),
  activity_session = NULL
)
}
\arguments{
\item{dn}{A \code{dynet} object.}

\item{enc}{Encoded edge list.}

\item{source}{Integer source.}

\item{lower, upper}{Query bounds; \code{upper} is the deadline and must be finite.}

\item{bounded}{Whether sessions are walls.}

\item{traversal_time}{Nonnegative duration per hop.}

\item{activity_mode, activity_session}{As in \code{\link[=.optimal_bounded_search]{.optimal_bounded_search()}}.}
}
\value{
A forward-shaped search object with an extra \code{departure} vector and
the per-target forward searches under \code{per_target}. \code{arrival} is
\code{NA_real_} for a supremum that no journey attains.
}
\description{
The latest time one can leave \code{source} and still reach a target \code{z} by
\code{upper} is exactly the latest-departure label that a backward search rooted
at \code{z} assigns to \code{source}. So this runs one backward search per target and
reads off the source's label, inheriting every session, activity, bound and
attainment rule unchanged. A forward search from that departure then
identifies the journeys of the latest-departing family: it is bounded above
by \code{upper}, so every journey it finds departs at or before the latest
departure, and it starts there, so every journey departs at or after it.
Its earliest arrival, hop count and path count are therefore those of the
latest-departing journeys, with the package's usual foremost-then-shortest
tie-break inside that family.
}
\examples{
dn <- dynet(school_contacts)
enc <- Dynet:::.encode(dn)
Dynet:::.latest_departure_search(dn, enc, 1L, 0, 10, FALSE)
}
\keyword{internal}
