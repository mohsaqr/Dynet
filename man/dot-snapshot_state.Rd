% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine.R
\name{.snapshot_state}
\alias{.snapshot_state}
\title{Build the common endpoint-valid state for one snapshot}
\usage{
.snapshot_state(dn, enc, bin, window, sessions, label)
}
\arguments{
\item{dn}{Parent \code{dynet} object.}

\item{enc}{Encoded observed edge rows.}

\item{bin}{One-row measurement window.}

\item{window}{Reporting-window width.}

\item{sessions}{Session aggregation mode.}

\item{label}{Current output-session label.}
}
\value{
A list with fixed-order \code{eligible}, endpoint-filtered edge \code{active},
and eligible integer \code{index}.
}
\description{
Positive windows independently union vertex and edge activity before
inducing; point windows evaluate both at the exact time. Bounded sessions
induce within each session before union, collapsed sessions erase labels
before induction, and separate sessions return one local state.
}
\examples{
dn <- dynet(data.frame(from = "A", to = "B", start = 0, end = 2))
enc <- Dynet:::.encode(dn)
Dynet:::.snapshot_state(dn, enc,
  data.frame(lo = 1, hi = 1, closed = TRUE, time = 1), 0,
  "bounded", "all")
}
\keyword{internal}
