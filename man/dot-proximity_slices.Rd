% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/proximity.R
\name{.proximity_slices}
\alias{.proximity_slices}
\title{Measure the network at a series of time slices}
\usage{
.proximity_slices(
  x,
  measure = "degree",
  slices = 120L,
  window = NULL,
  default_dist = 2
)
}
\arguments{
\item{x}{A \code{dynet} object.}

\item{measure}{Node-level measure to read off each slice.}

\item{slices}{Number of slices across the observation window, or \code{NULL} to
use the network's own time bins.}

\item{window}{Width of each slice. \code{NULL} uses \code{\link[=.default_window]{.default_window()}}.}

\item{default_dist}{Distance assumed between vertices with no path between
them.}
}
\value{
A list with \code{pos} and \code{weight} (both vertices-by-slices matrices),
\code{times}, \code{window} and \code{names}.
}
\description{
Each slice covers a window of real time. Its geodesic distances are reduced
to one dimension by classical scaling, so vertices that are interacting sit
near one another, and the node-level measure that drives line thickness is
read off the same window -- position and thickness therefore always describe
the same moment.
}
\details{
Slices may be closer together than they are wide, in which case consecutive
windows overlap and share most of their edges. That is what makes the
trajectories smooth: every drawn point is a real measurement of a real
window, and nothing between two measurements is invented.
}
\keyword{internal}
