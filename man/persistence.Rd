% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/persistence.R
\name{persistence}
\alias{persistence}
\title{Neighbourhood persistence between consecutive time bins}
\usage{
persistence(
  dn,
  scope = c("pertime", "node", "overall"),
  sessions = c("bounded", "collapse", "separate"),
  start = NULL,
  end = NULL,
  step = NULL,
  window = NULL,
  plot = FALSE
)
}
\arguments{
\item{dn}{A temporal network from \code{\link[=dynet]{dynet()}}.}

\item{scope}{\code{"pertime"}, the default, for one row per vertex per transition;
\code{"node"} for each vertex's mean over its transitions; \code{"overall"} for the
temporal correlation coefficient, one number per session.}

\item{sessions}{How to treat sessions, as in \code{\link[=dyn_centrality]{dyn_centrality()}}.}

\item{start, end, step, window}{The measurement grid, as in \code{\link[=snapshots]{snapshots()}}. A
transition is a consecutive pair of bins on that grid, so the grid decides
what "persisted" means; widening \code{step} makes persistence easier.}

\item{plot}{Whether to draw the result as well as return it. Drawing is a
side effect in the manner of \code{\link[graphics:hist]{graphics::hist()}}: the verb still returns
its tidy table, invisibly when it has drawn.}
}
\value{
A \code{dynet_metric}. Under \code{scope = "pertime"} it is \code{level = "node"}
with columns \code{time}, \code{node}, \code{measure} and \code{value}, one row per vertex per
transition, where \code{time} is the \strong{earlier} bin of the pair; the final bin
opens no transition and so contributes no rows at all, rather than a row
whose value is structurally undefined. Under \code{scope = "node"} it is
\code{level = "node"} with \code{node}, \code{measure} and \code{value}, one row per vertex.
Under \code{scope = "overall"} it is \code{level = "graph"} with \code{measure} and
\code{value}, one row per session. A leading \code{session} column is present under
\code{sessions = "separate"}. \code{measure} is \code{"topological_overlap"},
\code{"average_topological_overlap"} and \code{"temporal_correlation"} respectively,
and \code{value} lies in \verb{[0, 1]} at every scope. Print it, \code{\link[=summary]{summary()}} it,
\code{\link[=plot]{plot()}} it, or take the plain frame with \code{\link[=as.data.frame]{as.data.frame()}}.
}
\description{
Topological overlap: the share of a vertex's ties that survive from one time
bin to the next, normalised by the geometric mean of its degrees in the two
bins. Averaged over a vertex's transitions it is that vertex's persistence;
averaged again over vertices it is the network's temporal correlation
coefficient (Tang et al., 2010).
}
\details{
The measure is defined on undirected neighbourhoods, so a directed network
is read as a contact network here: each layer is folded onto its transpose
before the overlap is taken. Weights and spell counts are ignored -- a pair
is tied in a bin or it is not -- and loops are excluded.

A vertex isolated in either bin of a transition scores zero rather than a
missing value. That is a substantive claim, not an arithmetic accident: the
ratio is genuinely 0/0, and zero is the convention teneto uses. A reader who
wants isolation and genuine turnover distinguished should read the degree
alongside, which \code{\link[=dyn_centrality]{dyn_centrality()}} gives.
}
\section{Conditions}{

Errors: \code{dynet_empty_result} when the grid yields fewer than two bins, since
a single bin opens no transition and the measure is undefined;
\code{dynet_no_sessions} when \code{sessions = "separate"} is asked of a network with
no session column; and \code{dynet_bad_input} when \code{dn} is not a \code{dynet}.
}

\examples{
dn <- dynet(school_contacts)
kept <- persistence(dn)
kept

per_node <- persistence(dn, scope = "node")
per_node

overall <- persistence(dn, scope = "overall")
overall

}
\references{
Tang, J., Scellato, S., Musolesi, M., Mascolo, C., & Latora, V. (2010).
Small-world behavior in time-varying graphs. \emph{Physical Review E}, 81(5),
055101(R). \doi{10.1103/PhysRevE.81.055101}

Nicosia, V., Tang, J., Mascolo, C., Musolesi, M., Russo, G., & Latora, V.
(2013). Graph metrics for temporal networks. In P. Holme & J. Saramaki
(Eds.), \emph{Temporal Networks} (pp. 15-40). Springer.
\doi{10.1007/978-3-642-36461-7_2}

Clauset, A., & Eagle, N. (2007). Persistence and periodicity in a dynamic
proximity network. \emph{DIMACS Workshop on Computational Methods for Dynamic
Interaction Networks}.
}
\seealso{
\code{\link[=similarity]{similarity()}}, which compares whole edge sets between every pair
of bins and answers a different question, and \code{\link[=turnover]{turnover()}} for the
complementary view of what changed.
}
