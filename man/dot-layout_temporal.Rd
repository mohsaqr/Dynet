% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme.R
\name{.layout_temporal}
\alias{.layout_temporal}
\title{Position vertices by arrival time and path depth}
\usage{
.layout_temporal(network, ...)
}
\arguments{
\item{network}{A \code{CographNetwork} carrying the two columns above.}

\item{...}{Ignored; present because cograph passes layout parameters
through.}
}
\value{
A \code{data.frame} with \code{x} and \code{y}, one row per vertex.
}
\description{
A layout for cograph's layout registry. Horizontal position is the real
arrival time of a vertex and vertical position is its depth in hops, so
the two axes can disagree: a vertex reached in one hop but late sits to
the right of one reached in four hops but early. A tree drawn by ordinary
graph-drawing rules hides exactly that.
}
\details{
The network must carry \code{arrival_time} and \code{n_hops} vertex columns, which
\code{\link[=plot.dynet_paths]{plot.dynet_paths()}} writes when it builds the tree.
}
\keyword{internal}
