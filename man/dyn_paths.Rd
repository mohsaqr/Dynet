% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paths.R
\name{dyn_paths}
\alias{dyn_paths}
\title{Time-respecting paths from a vertex}
\usage{
dyn_paths(
  dn,
  from,
  at = NULL,
  direction = c("forward", "backward"),
  sessions = c("bounded", "collapse", "separate")
)
}
\arguments{
\item{dn}{A temporal network from \code{\link[=dynet]{dynet()}}.}

\item{from}{Name of the vertex to start from.}

\item{at}{Forward source-availability time or backward arrival deadline.
Defaults to the start of the observation window for forward paths and its
end for backward paths. Date and date-time values use the network's time
scale.}

\item{direction}{\code{"forward"} traces where the vertex can reach;
\code{"backward"} traces who could have reached it.}

\item{sessions}{How to treat sessions, as in \code{\link[=dyn_centrality]{dyn_centrality()}}.}
}
\value{
An object of class \code{"dynet_paths"}: a tidy data frame with one row
per vertex and columns \code{node}, \code{reachable}, \code{arrival_time}, \code{attained}
(whether that optimum itself is realized), \code{latency} (time taken from the
source), \code{n_hops} and \code{previous} (the adjacent vertex on the reported
journey, by name).
}
\description{
Follows every time-respecting path out of (or into) one vertex and reports
where it gets to, when, and through whom. A path may only use edges whose
timing runs forward, so unlike a path in a flattened network it can never
travel back in time.

The source vertex is named, not numbered. \code{dyn_paths(dn, from = "Ana")}
works; there is no vertex index to look up first.

Forward paths use nondecreasing traversal times, so relations active at the
same instant may form a multi-hop chain. Waiting is allowed. Interval spells
are onset-inclusive and terminus-exclusive; point events are traversable at
their exact timestamp through a distinct event rule. Reach and arrival do
not depend on edge-row order or duplicate spell rows.
}
\details{
A valid forward journey has distinct vertices and traversal times
\verb{at <= t1 <= ... <= tk}. At each hop, either an interval is active at the
traversal time or a point event occurs exactly then. The empty journey
reaches the source at \code{at}. Cycles are unnecessary for reach and earliest
arrival because deleting a repeated-vertex section and waiting at that
vertex preserves every later hop. This contract defines feasibility and
arrival; optimal-journey ties and multiplicity are separate quantities.

For backward paths, \code{arrival_time} is the latest-departure supremum for a
journey ending at the named target by \code{at}, and \code{latency} is \code{at} minus that
value. A supremum at an interval's excluded terminus need not itself be an
attainable departure. The \code{previous} column points one hop onward toward
the target.
}
\examples{
dn <- dynet(school_contacts)
dyn_paths(dn, from = "Ana")
summary(dyn_paths(dn, from = "Ana"))

}
\references{
Kempe, D., Kleinberg, J., & Kumar, A. (2002). Connectivity and inference
problems for temporal networks. \emph{Journal of Computer and System Sciences},
64(4), 820-842.

Holme, P., & Saramaki, J. (2012). Temporal networks. \emph{Physics Reports},
519(3), 97-125.

Casteigts, A., Corsini, A., & Sarkar, W. (2024). Simple, strict, proper,
happy: A study of reachability in temporal graphs. \emph{Theoretical Computer
Science}, 991, 114434.
}
