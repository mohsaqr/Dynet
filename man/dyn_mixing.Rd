% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mixing.R
\name{dyn_mixing}
\alias{dyn_mixing}
\title{Mixing between vertex groups over time}
\usage{
dyn_mixing(
  dn,
  attribute,
  sessions = c("bounded", "collapse", "separate"),
  sample = NULL,
  start = NULL,
  end = NULL,
  step = NULL,
  window = NULL
)
}
\arguments{
\item{dn}{A temporal network from \code{\link[=dynet]{dynet()}} built with vertex attributes.}

\item{attribute}{Name of a column in the vertex table.}

\item{sessions}{How to treat sessions, as in \code{\link[=dyn_centrality]{dyn_centrality()}}.}

\item{sample}{Deprecated. \code{"instant"} is equivalent to \code{window = 0};
\code{"window"} uses the current positive/default window.}

\item{start, end}{First and last time at which to measure. Default to the
observed range. A network built from dates may be addressed with dates.}

\item{step}{How often to measure. Defaults to the interval the network was
built with.}

\item{window}{How much time each measurement covers. Defaults to \code{step},
which tiles the period into disjoint bins. A larger value slides an
overlapping window; \code{0} samples the network at each point in time.}
}
\value{
A \code{dynet_metric} at graph level with one row per time point and
ordered group pair. Alongside \code{measure} (rendered as \code{"A -> B"}) and
\code{value} (the edge count) it carries \code{from_group} and \code{to_group} columns,
so a single pairing can be pulled out with \code{\link[=subset]{subset()}}.
}
\description{
How much each kind of vertex interacted with each other kind, in every time
bin. This is the question a temporal network answers that a static one
cannot: not whether high and low achievers mixed, but \emph{when} they did, and
whether the pattern held or decayed.

The grouping variable comes from the vertex attributes supplied to
\code{\link[=dynet]{dynet()}} through its \code{nodes} argument.
}
\examples{
dn <- dynet(forum_posts, thread = "thread", nodes = forum_people)
dyn_mixing(dn, attribute = "role")
plot(dyn_mixing(dn, attribute = "role"))

}
