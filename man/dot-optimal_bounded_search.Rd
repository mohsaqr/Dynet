% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paths.R
\name{.optimal_bounded_search}
\alias{.optimal_bounded_search}
\title{Run an optimal path search with optional session walls}
\usage{
.optimal_bounded_search(
  dn,
  enc,
  source,
  origin,
  direction,
  bounded,
  lower = -Inf,
  upper = Inf,
  traversal_time = 0,
  activity_mode = c("collapse", "separate"),
  activity_session = NULL,
  criterion = "foremost_then_shortest",
  max_states = 1e+05,
  origin_attained = TRUE,
  table_cache = NULL
)
}
\arguments{
\item{dn}{A \code{dynet} object.}

\item{enc}{Encoded edge list.}

\item{source}{Integer source/target.}

\item{origin}{Query anchor.}

\item{direction}{Search direction.}

\item{bounded}{Whether sessions are walls.}

\item{lower, upper}{Query bounds.}

\item{traversal_time}{Nonnegative duration per hop.}

\item{activity_mode, activity_session}{Session handling for vertex activity.}

\item{criterion}{Which optimisation problem to solve.}

\item{max_states}{State budget for \code{criterion = "foremost"}.}

\item{origin_attained}{Whether the anchor is ready exactly at \code{origin}.}

\item{table_cache}{An environment in which atom and domain tables are
shared across the searches of one query, or \code{NULL}.}
}
\value{
A direct or session-envelope optimal search.
}
\description{
Run an optimal path search with optional session walls
}
\examples{
dn <- dynet(school_contacts)
enc <- Dynet:::.encode(dn)
Dynet:::.optimal_bounded_search(
  dn, enc, 1L, 0, "forward", FALSE, upper = 10
)
}
\keyword{internal}
