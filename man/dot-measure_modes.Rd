% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/centrality.R
\name{.measure_modes}
\alias{.measure_modes}
\title{Expand measures across directions into one job per output column}
\usage{
.measure_modes(measure, mode, directed)
}
\arguments{
\item{measure}{Character vector of measures.}

\item{mode}{Character vector of resolved directions.}

\item{directed}{Whether the network is directed.}
}
\value{
A data frame with \code{measure}, \code{mode} and \code{label}, one row per column
the result will carry.
}
\description{
Asking for in- and out-degree in one call must not silently return two
identically named stacks, and asking a direction-blind measure for three
directions must not compute it three times. Directions are therefore
dropped where they mean nothing, and the surviving ones are suffixed only
when more than one direction was requested -- so a single-\code{mode} call keeps
the plain measure name it has always had.
}
\examples{
Dynet:::.measure_modes("degree", c("in", "out"), TRUE)
Dynet:::.measure_modes(c("degree", "betweenness"), c("all", "in"), TRUE)
}
\keyword{internal}
