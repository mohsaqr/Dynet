% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/path-trajectory.R
\name{path_trajectories}
\alias{path_trajectories}
\title{Optimal temporal routes as a counted trajectory tree}
\usage{
path_trajectories(x, min_count = 1L, plot = FALSE)
}
\arguments{
\item{x}{A result from \code{\link[=paths]{paths()}}.}

\item{min_count}{Keep only branches used by at least this many optimal
routes. The default of \code{1} keeps the complete family; a higher value is
the caller's explicit pruning.}

\item{plot}{Whether to draw the result as well as return it. Drawing is a
side effect in the manner of \code{\link[graphics:hist]{graphics::hist()}}: the verb still returns
its tidy table, invisibly when it has drawn, so \code{plot = TRUE} saves the
wrapping \code{plot()} call without changing what comes back. Use \code{plot()} on
the result when the figure needs arguments of its own.}
}
\value{
A \code{dynet_path_trajectories} data frame with one row per tree node
and columns \code{node} (the route prefix, written as \code{vertex@time} steps
joined by arrows), \code{parent}, \code{depth}, \code{count}, \code{probability}, \code{vertex},
\code{time}, \code{session} and \code{branch}. \code{depth} is the hop number from the
queried vertex, \code{probability} is the branching fraction of the parent's
routes that continue along this branch and is missing at the root, which
has no parent, and \code{branch} is the node's
placement across the tree. The synthetic \code{(start)} root is dropped when
it has a single child, which is the usual case; it is kept when it
genuinely branches, as under \code{sessions = "separate"}, where it carries
one subtree per session, has no \code{vertex} or \code{time}, a missing
\code{probability}, and pushes every other node one hop deeper.

A result that is not from \code{\link[=paths]{paths()}}, or a \code{min_count} that is not one
positive whole number, raises \code{dynet_bad_input}; a path result with no
route step, or a \code{min_count} no prefix reaches, raises
\code{dynet_empty_result}.
}
\description{
Turns the optimal route family returned by \code{\link[=paths]{paths()}} into a tidy
prefix tree. Every row is one tree node: a route prefix reaching \code{vertex}
at \code{time}, used by \code{count} optimal routes. A named vertex reached through
a different temporal history is a separate row, so branches never create
the misleading crossings of a path-union graph and two routes that differ
only in when a hop fires stay separate.

Forward routes grow away from the queried source. Backward routes are
reversed, so the queried target is the root and possible senders branch
away from it.
}
\examples{
dn <- dynet(school_contacts)
routes <- paths(dn, from = "Ana")
path_trajectories(routes)
}
\seealso{
\code{\link[=plot_path_trajectories]{plot_path_trajectories()}} to draw the tree, \code{\link[=path_network]{path_network()}} for
the route union as a network.
}
