% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/animate.R
\name{as.data.frame.dynet_animation}
\alias{as.data.frame.dynet_animation}
\title{Coerce an animation to a data frame}
\usage{
\method{as.data.frame}{dynet_animation}(
  x,
  row.names = NULL,
  optional = FALSE,
  what = c("bins", "frames"),
  ...
)
}
\arguments{
\item{x}{A \code{dynet_animation} from \code{\link[=animate]{animate()}}.}

\item{row.names, optional}{As in \code{\link[base:as.data.frame]{base::as.data.frame()}}.}

\item{what}{\code{"bins"}, the default, for one row per bin with the columns
\code{\link[=animate]{animate()}} documents; \code{"frames"} for one row per rendered frame,
with \code{frame}, \code{bin} (the bin the frame belongs to), \code{phase} (how far
along the transition to the next bin, in \verb{[0, 1)}) and \code{time} (where the
timeline marker stands).}

\item{...}{Ignored.}
}
\value{
A plain \code{data.frame}.
}
\description{
Coerce an animation to a data frame
}
\examples{
if (requireNamespace("gifski", quietly = TRUE) &&
  requireNamespace("cograph", quietly = TRUE)) {
  dn <- dynet(school_contacts)
  frames <- animate(dn, step = 6, window = 6, tween = 2)
  as.data.frame(frames)
  as.data.frame(frames, what = "frames")
}
}
