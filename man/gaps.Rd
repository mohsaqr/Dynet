% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/events.R
\name{gaps}
\alias{gaps}
\title{Inter-event gaps}
\usage{
gaps(
  dn,
  unit = c("node", "pair"),
  sessions = c("bounded", "collapse", "separate"),
  censored = c("exclude", "include"),
  plot = FALSE
)
}
\arguments{
\item{dn}{A temporal network from \code{\link[=dynet]{dynet()}}.}

\item{unit}{\code{"node"}, the default, for the gaps between the events incident
to each vertex, a loop counting once and direction ignored; \code{"pair"} for
the gaps between the events of each ordered pair, or each unordered dyad
on an undirected network. Loops are excluded from \code{"pair"}, matching
\code{\link[=durations]{durations()}}.}

\item{sessions}{How to treat sessions, as in \code{\link[=burstiness]{burstiness()}}. \code{"bounded"},
the default, forms gaps inside each session so no gap spans a session
wall; \code{"collapse"} erases the labels and pools one calendar sequence;
\code{"separate"} reports each session on its own rows and needs a network
built with a session column.}

\item{censored}{\code{"exclude"}, the default, drops events whose onset is
censored, which is what \code{\link[=burstiness]{burstiness()}} does, so the mean of these gaps is
its \code{"mean_gap"}. \code{"include"} keeps them, at the cost of gaps measured
from an onset that was never observed. Note the default is the opposite
of \code{\link[=durations]{durations()}}, whose \code{"include"} default is about retaining spells
rather than trusting their onsets.}

\item{plot}{Whether to draw the result as well as return it. Drawing is a
side effect in the manner of \code{\link[graphics:hist]{graphics::hist()}}: the verb still returns
its tidy table, invisibly when it has drawn.}
}
\value{
A \code{dynet_metric} data frame with one row per gap and no
aggregation, \code{level = "node"} under \code{unit = "node"} and \code{level = "edge"}
under \code{unit = "pair"}. Columns are \code{time}, the onset of the later event of
the pair, so the row sits at the moment the gap closed; \code{node}, or \code{from}
and \code{to} under \code{unit = "pair"}; \code{index}, the 1-based rank of the gap
within that vertex's or pair's ordered sequence, restarting at one in each
session under \code{"bounded"} and \code{"separate"}; \code{measure}, the constant
\code{"gap"}; and \code{value}, the gap in the network's time unit. A leading
\code{session} column is present under \code{sessions = "separate"}. A vertex or
pair with fewer than two usable events contributes no rows rather than a
missing one. Print it, \code{\link[=summary]{summary()}} it, \code{\link[=plot]{plot()}} it, or take the plain
frame with \code{\link[=as.data.frame]{as.data.frame()}}.
}
\description{
Every interval between one event and the next, as a tidy table with one row
per gap. \code{burstiness()} computes these gaps to reach \code{B} and \code{M} and then
reports only their mean; this returns the distribution itself, so it can be
plotted, fitted, or tested against a Poisson null.
}
\details{
A gap of exactly zero is legitimate and is kept: it is the signature of two
distinct raw spells incident to the same vertex at one instant. Dropping
those would silently raise the mean above the \code{"mean_gap"} that
\code{\link[=burstiness]{burstiness()}} reports from the same numbers.
}
\section{Conditions}{

Errors: \code{dynet_bad_input} when \code{dn} is not a \code{dynet}, and
\code{dynet_no_sessions} when \code{sessions = "separate"} is asked of a network with
no session column. An unmatched \code{unit}, \code{sessions} or \code{censored} is
rejected by \code{\link[=match.arg]{match.arg()}} and is a plain error, not a classed one.
}

\examples{
dn <- dynet(school_contacts)
quiet <- gaps(dn)
quiet
summary(quiet)

dyads <- gaps(dn, unit = "pair")
dyads

}
\references{
Goh, K.-I., & Barabasi, A.-L. (2008). Burstiness and memory in complex
systems. \emph{Europhysics Letters}, 81(4), 48002. \doi{10.1209/0295-5075/81/48002}

Karsai, M., Kaski, K., Barabasi, A.-L., & Kertesz, J. (2012). Universal
features of correlated bursty behaviour. \emph{Scientific Reports}, 2, 397.
\doi{10.1038/srep00397}

Holme, P., & Saramaki, J. (2012). Temporal networks. \emph{Physics Reports},
519(3), 97-125. \doi{10.1016/j.physrep.2012.03.001}
}
\seealso{
\code{\link[=burstiness]{burstiness()}} for the summary statistics of these gaps, and
\code{\link[=durations]{durations()}} for how long a tie was up rather than how long a pair was
quiet.
}
