% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{.splot_args}
\alias{.splot_args}
\title{Dynet's rendering defaults, with anything the caller set taking precedence}
\usage{
.splot_args(net, user, palette = "okabe")
}
\arguments{
\item{net}{The netobject about to be drawn.}

\item{user}{List of arguments supplied by the caller.}

\item{palette}{Palette specification, as in \code{\link[=plot.dynet]{plot.dynet()}}.}
}
\value{
A list of arguments for \code{cograph::splot()}.
}
\description{
Colour is carried by the registered \code{"dynet"} theme (see
\code{\link[=.register_dynet_theme]{.register_dynet_theme()}}); everything the theme contract cannot express
is stated here. Two things make the extra layer necessary.
}
\details{
First, \code{cograph::splot()} treats a directed netobject carrying no \verb{$method}
as a transition network and applies its TNA look: a per-state colour ramp
and a numeric label on every edge. That is right for a transition matrix
and wrong here, where an edge weight is a count of meetings. The TNA block
also runs \emph{before} the theme block and fills \code{node_fill}, which the theme
block then declines to overwrite -- so a theme alone cannot undo it and
\code{tna_styling = FALSE} is required.

Second, a theme holds a single \code{node_fill}. Colouring vertices by their
partition needs a vector, so that is supplied here instead, and only when
there is a partition -- leaving a plain network free to take its fill from
whichever theme the caller asked for.

Everything in \code{...} wins, which is the same delegation contract
\code{lagdynamics::plot_transitions()} uses.
}
\keyword{internal}
