% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/centrality.R
\name{.eigen_prestige}
\alias{.eigen_prestige}
\title{Incoming binary Perron prestige with uniqueness certification}
\usage{
.eigen_prestige(
  a,
  rescale = FALSE,
  definition = c("eigenvector", "eigenvector.rownorm", "eigenvector.colnorm",
    "eigenvector.rowcolnorm"),
  tol = 1e-10,
  balance_tol = 1e-12,
  balance_max_iter = 10000L,
  warn = TRUE
)
}
\arguments{
\item{a}{Directed adjacency matrix for one snapshot block. Positive values
are treated as binary dyads; retained loops remain on the diagonal.}

\item{rescale}{Whether to normalize the feasible Perron vector to sum one.}

\item{definition}{Ordinary \code{"eigenvector"}, \code{"eigenvector.rownorm"},
which row-normalizes binary adjacency, \code{"eigenvector.colnorm"}, which
column-normalizes it, or \code{"eigenvector.rowcolnorm"}, which requires total
support and deterministic doubly stochastic balancing before transpose
and eigensolving.}

\item{tol}{Fixed relative tolerance for root, nullity, sign, and residual
checks.}

\item{balance_tol}{Internal S09 maximum absolute margin error.}

\item{balance_max_iter}{Internal S09 cap on complete balancing sweeps.}

\item{warn}{Whether an undefined direct call emits its classed warning.
Public calls aggregate warnings after all reporting blocks.}
}
\value{
A numeric vector with L2 norm or sum one. Undefined blocks are all
\code{NA} and carry a \code{prestige_diagnostic} attribute.
}
\description{
For binary directed adjacency \code{B}, ordinary eigenvector prestige solves
\code{t(B) \%*\% p = rho(B) * p}. The row-normalized definition first forms
\code{P[i,j] = B[i,j] / sum(B[i,])} for positive row totals, leaving zero rows
exactly zero. The column-normalized definition analogously forms
\code{Q[i,j] = B[i,j] / sum(B[,j])} and leaves zero columns zero. The row-column
definition first certifies total support and balances binary \code{B} to doubly
stochastic form. Each then solves the transposed Perron equation on its
transformed matrix. A score is
published only when the spectral radius is positive and the Perron
eigenspace is one-dimensional. Raw values have Euclidean norm one;
optional rescaling makes their sum one. Zero-radius and nonunique blocks
return \code{NA} with a diagnostic rather than an arbitrary eigenvector.
}
\examples{
cycle <- matrix(c(0, 1, 0, 0, 0, 1, 1, 0, 0), 3, 3, byrow = TRUE)
Dynet:::.eigen_prestige(cycle)
Dynet:::.eigen_prestige(cycle, rescale = TRUE)
Dynet:::.eigen_prestige(cycle, definition = "eigenvector.rownorm")
Dynet:::.eigen_prestige(cycle, definition = "eigenvector.colnorm")
Dynet:::.eigen_prestige(cycle, definition = "eigenvector.rowcolnorm")
}
\references{
Bonacich, P. (1972). Factoring and weighting approaches to status scores
and clique identification. \emph{Journal of Mathematical Sociology}, 2, 113-120.
doi:10.1080/0022250X.1972.9989806.

Berman, A., & Plemmons, R. J. (1994). \emph{Nonnegative Matrices in the
Mathematical Sciences}. SIAM. doi:10.1137/1.9781611971262.
}
\keyword{internal}
