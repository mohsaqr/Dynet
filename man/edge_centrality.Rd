% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/centrality.R
\name{edge_centrality}
\alias{edge_centrality}
\title{Temporal centrality of the contacts themselves}
\usage{
edge_centrality(
  dn,
  measure = "betweenness",
  criterion = c("foremost_then_shortest", "min_hops", "foremost", "fastest"),
  sessions = c("bounded", "collapse", "separate"),
  start = NULL,
  end = NULL,
  traversal_time = 0
)
}
\arguments{
\item{dn}{A temporal network from \code{\link[=dynet]{dynet()}}.}

\item{measure}{Currently \code{"betweenness"}.}

\item{criterion}{Which optimisation problem the credited journeys solve, as
in \code{\link[=paths]{paths()}}. \code{"foremost"} is refused with \code{dynet_intractable_criterion}:
crediting contacts needs the count of every vertex-simple foremost
journey, which is #P-hard.}

\item{sessions}{Session aggregation policy.}

\item{start, end}{First and last time to search.}

\item{traversal_time}{Nonnegative duration charged for every hop.}
}
\value{
A \code{dynet_metric} at edge level, one row per contact, with columns
\code{from}, \code{to}, \code{start}, \code{end}, \code{measure} and \code{value}. A contact used by no
optimal journey is present with value zero rather than dropped, so the
result is a complete census.
}
\description{
Credits every optimal time-respecting journey to the contacts that carried
it, giving a betweenness score per contact rather than per vertex. This is
the measure intervention questions actually ask: not which people matter,
but which meetings did.

The row unit is one canonical contact, not one pair. The same \code{A -> B} pair
active in two disjoint spells is two rows, because a journey uses one of
them and not the other.
}
\details{
The score is not normalised, and its range is the same \verb{[0, (n-1)(n-2)]} as
the vertex measure.

An exact identity ties this to \code{\link[=dyn_centrality]{dyn_centrality()}}: because every optimal
journey is vertex-simple, it enters each vertex through exactly one contact,
so the scores of the contacts arriving at a vertex sum to that vertex's
temporal betweenness plus the number of sources that reach it. The tests
assert it, which makes this verb checkable without any external reference.
}
\examples{
dn <- dynet(school_contacts)
edge_centrality(dn)
}
\references{
Oettershagen, L., and Mutzel, P. (2022). TGLib: an open-source library for
temporal graph analysis. \emph{ICDM Workshops}. arXiv:2209.12587.

Brandes, U. (2001). A faster algorithm for betweenness centrality. \emph{Journal
of Mathematical Sociology}, 25(2), 163-177.
}
\seealso{
\code{\link[=dyn_centrality]{dyn_centrality()}} for the vertex form.
}
