% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate.R
\name{random_dynet}
\alias{random_dynet}
\title{Simulate a random temporal network}
\usage{
random_dynet(
  nodes = 20L,
  times = 20L,
  model = c("binomial", "poisson", "block", "activation"),
  p = 0.1,
  birth = NULL,
  persist = NULL,
  rate = 1,
  blocks = 2L,
  p_within = 0.3,
  p_between = 0.02,
  block_switch = 0,
  waiting = c("exponential", "weibull", "lognormal"),
  shape = 1,
  directed = TRUE,
  interval = 1,
  loops = FALSE,
  seed = NULL
)
}
\arguments{
\item{nodes}{A vertex count, or a character vector of names. A count
generates zero-padded names so that lexical and numeric order agree.}

\item{times}{Number of slices for the slice models, or the length of the
observation window for the event models.}

\item{model}{\code{"binomial"} for independent per-slice edges, \code{"poisson"} for
a renewal process on every dyad, \code{"block"} for a planted partition, or
\code{"activation"} for a static graph whose links activate as a renewal
process with tunable burstiness.}

\item{p}{Per-slice edge probability for \code{"binomial"}, or the density of the
underlying static graph for \code{"activation"}.}

\item{birth, persist}{The two-state Markov variant of \code{"binomial"}:
probability an inactive dyad becomes active, and probability an active one
stays active. Named \code{persist}, not a death rate, because that is what it
is. Supplying either requires both and forbids \code{p}.}

\item{rate}{Mean events per unit time for the event models.}

\item{blocks}{A block count, or one block label per vertex.}

\item{p_within, p_between}{Per-slice edge probability inside and between
blocks.}

\item{block_switch}{Per-slice probability a vertex changes block, so the
planted partition drifts. Zero gives a static partition.}

\item{waiting, shape}{The renewal gap distribution for \code{"activation"}.
Weibull with \code{shape < 1} is bursty, \code{shape > 1} regular, \code{shape = 1}
Poisson. The scale is set so the mean gap is \code{1 / rate} at every shape.}

\item{directed, interval, loops}{Passed through to \code{\link[=dynet]{dynet()}}.}

\item{seed}{A single whole number for a reproducible draw, or \code{NULL}.}
}
\value{
A \code{dynet}, so every verb, plot and accessor applies with no special
casing. \code{"binomial"} and \code{"block"} produce interval networks;
\code{"poisson"} and \code{"activation"} produce contact networks. For \code{"block"}
the planted partition is written into the node table as \code{block}, so
\code{mixing(dn, attribute = "block")} reads it with no further argument.
}
\description{
Generates a temporal network with a known generating process, so a measure
can be checked against an answer that is known in advance rather than only
against another implementation. A Poisson process has burstiness exactly
zero; a binomial network has expected per-slice density exactly \code{p}; a block
network carries its planted partition as a node attribute.
}
\details{
The slice models draw a dyad-by-slice activity matrix and then collapse each
maximal run of consecutive active slices into one spell. The generated
network therefore has fewer, longer spells than the model has active slices,
which is what makes it a spell network rather than a stack of snapshots.

\code{"poisson"} and \code{"activation"} use \strong{exponential} waiting times, so they are
genuine renewal processes. teneto's \code{rand_poisson} draws integer gaps that
can be zero, which is not a Poisson process and is not numerically
comparable with these.

\strong{Reading burstiness off a generated network.} \code{\link[=burstiness]{burstiness()}} is a
node-level measure: it pools the events of every dyad incident to a vertex.
Superposing independent renewal processes drives the pooled process toward
Poisson, so a vertex with many partners scores near zero however bursty each
of its links is. Measured here at \code{shape = 0.5}, whose per-link burstiness is
0.382: a vertex with one incident process scored 0.36, with three 0.26, with
seven 0.19 and with fifteen 0.14. To calibrate against the per-link value,
generate \verb{nodes = 2, directed = FALSE}, which gives each vertex exactly one
process. This is a property of the measure, not of the generator.

Weibull waiting has a closed-form burstiness,
\eqn{B = (\sigma - \mu)/(\sigma + \mu)} with \eqn{\mu = \Gamma(1 + 1/k)}
and \eqn{\sigma^2 = \Gamma(1 + 2/k) - \mu^2}, giving 0.381966 at
\code{shape = 0.5}, 0 at \code{shape = 1} and -0.313436 at \code{shape = 2}. The generated
network approaches these from a finite window; \code{shape = 0.5} converges most
slowly because its gaps are heavy-tailed.
}
\examples{
random_dynet(nodes = 10, times = 12, model = "binomial", p = 0.2, seed = 1)
random_dynet(nodes = 8, times = 50, model = "poisson", rate = 0.5, seed = 1)
random_dynet(nodes = 12, times = 15, model = "block", blocks = 2, seed = 1)
}
\references{
Erdos, P., and Renyi, A. (1959). On random graphs I. \emph{Publicationes
Mathematicae}, 6, 290-297.

Holland, P. W., Laskey, K. B., and Leinhardt, S. (1983). Stochastic
blockmodels: first steps. \emph{Social Networks}, 5(2), 109-137.

Goh, K.-I., and Barabasi, A.-L. (2008). Burstiness and memory in complex
systems. \emph{Europhysics Letters}, 81(4), 48002.

Vazquez, A., Oliveira, J. G., Dezso, Z., Goh, K.-I., Kondor, I., and
Barabasi, A.-L. (2006). Modeling bursts and heavy tails in human dynamics.
\emph{Physical Review E}, 73, 036127.
}
\seealso{
\code{\link[=randomise]{randomise()}} for destroying structure in an observed network
rather than generating it.
}
