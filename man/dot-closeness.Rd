% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kernels.R
\name{.closeness}
\alias{.closeness}
\title{Normalised closeness centrality}
\usage{
.closeness(a, directed = TRUE)
}
\arguments{
\item{a}{Adjacency matrix.}

\item{directed}{Whether to respect direction.}
}
\value{
A named numeric vector.
}
\description{
The number of vertices a vertex can reach, divided by the total distance
to them. This matches \code{igraph::closeness(normalized = TRUE)} and stays
informative on a disconnected graph, which every snapshot of a temporal
network is. \code{sna::closeness()} instead sums over unreachable vertices too
and therefore collapses to zero for the whole graph as soon as one vertex
is unreachable.
}
\details{
A vertex that reaches nothing is reported as zero rather than \code{NaN}, so
that averaging a closeness series does not silently propagate a missing
value.
}
\keyword{internal}
