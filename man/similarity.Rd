% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/similarity.R
\name{similarity}
\alias{similarity}
\title{Similarity between the networks at each pair of time points}
\usage{
similarity(
  dn,
  method = c("jaccard", "overlap", "hamming", "cosine", "pearson"),
  sessions = c("bounded", "collapse", "separate"),
  start = NULL,
  end = NULL,
  step = NULL,
  window = NULL,
  plot = FALSE
)
}
\arguments{
\item{dn}{A temporal network from \code{\link[=dynet]{dynet()}}.}

\item{method}{One of \code{"jaccard"} (the default), \code{"overlap"}, \code{"hamming"},
\code{"cosine"} or \code{"pearson"}.}

\item{sessions}{How to treat sessions when the layers are built:
\code{"bounded"} (the default) and \code{"collapse"} differ in whether a session
wall gates a tie into its bin. Unlike \code{\link[=dyn_centrality]{dyn_centrality()}}, \code{"separate"}
adds no \code{session} column here: layers are keyed on time alone, so two
session-local bins sharing a time are compared as one layer.}

\item{start, end}{First and last time to measure. Default to the observed
range.}

\item{step}{How often to measure. Defaults to the interval the network was
built with.}

\item{window}{How much time each measurement covers. Defaults to \code{step}.
\code{window = "all"} measures the whole observed period as a single bin and so
leaves nothing to compare; it raises \code{dynet_empty_result}, as does any
other grid that yields fewer than two bins.}

\item{plot}{Whether to draw the result as well as return it. Drawing is a
side effect in the manner of \code{\link[graphics:hist]{graphics::hist()}}: the verb still returns
its tidy table, invisibly when it has drawn, so \code{plot = TRUE} saves the
wrapping \code{plot()} call without changing what comes back. Use \code{plot()} on
the result when the figure needs arguments of its own.}
}
\value{
A \code{dynet_similarity} data frame with one row per ordered pair of
time bins and columns \code{time}, \code{other}, \code{measure} and \code{value}. The
diagonal is included and is one for every coefficient except
\code{"hamming"}, where identical layers differ in nothing and score zero.
\code{"pearson"} reaches one only to floating-point accuracy, so compare it
with a tolerance rather than with \code{==}. The frame is returned invisibly
when \code{plot = TRUE} has drawn the figure.

The coefficients come from cograph, which is a hard dependency of Dynet;
a namespace that cannot be loaded raises \code{dynet_needs_cograph}.
}
\description{
Compares the edge set at every time bin with the edge set at every other,
giving the pairwise similarity matrix as a tidy frame. This answers how
much the network at one moment resembles the network at another, which no
single-bin measure reports and which the formation and dissolution
quantities in \code{\link[=events]{events()}} only address between neighbouring bins.

Coefficients are computed by \code{cograph::layer_similarity()}.
}
\examples{
dn <- dynet(school_contacts)
similarity(dn)
similarity(dn, method = "cosine")
}
\seealso{
\code{\link[=snapshots]{snapshots()}} for the networks being compared, \code{\link[=events]{events()}} for
formation and dissolution between neighbouring bins.
}
