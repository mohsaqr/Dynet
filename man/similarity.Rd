% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/similarity.R
\name{similarity}
\alias{similarity}
\title{Similarity between the networks at each pair of time points}
\usage{
similarity(
  dn,
  method = c("jaccard", "overlap", "hamming", "cosine", "pearson"),
  sessions = c("bounded", "collapse", "separate"),
  start = NULL,
  end = NULL,
  step = NULL,
  window = NULL
)
}
\arguments{
\item{dn}{A temporal network from \code{\link[=dynet]{dynet()}}.}

\item{method}{One of \code{"jaccard"} (the default), \code{"overlap"}, \code{"hamming"},
\code{"cosine"} or \code{"pearson"}.}

\item{sessions}{How to treat sessions, as in \code{\link[=dyn_centrality]{dyn_centrality()}}.}

\item{start, end}{First and last time to measure. Default to the observed
range.}

\item{step, window}{How often to measure and how much time each measurement
covers. Default to the interval the network was built with. \code{window = "all"} is rejected here, because a similarity matrix of one bin against
itself says nothing.}
}
\value{
A \code{dynet_similarity} data frame with one row per ordered pair of
time bins and columns \code{time}, \code{other}, \code{measure} and \code{value}. The
diagonal is included and is one for every coefficient except
\code{"hamming"}, where identical layers differ in nothing and score zero.
\code{"pearson"} reaches one only to floating-point accuracy, so compare it
with a tolerance rather than with \code{==}.
}
\description{
Compares the edge set at every time bin with the edge set at every other,
giving the pairwise similarity matrix as a tidy frame. This answers how
much the network at one moment resembles the network at another, which no
single-bin measure reports and which the formation and dissolution
quantities in \code{\link[=events]{events()}} only address between neighbouring bins.

Coefficients are computed by \code{cograph::layer_similarity()}.
}
\examples{
dn <- dynet(school_contacts)
similarity(dn)
similarity(dn, method = "cosine")
}
\seealso{
\code{\link[=snapshots]{snapshots()}} for the networks being compared, \code{\link[=events]{events()}} for
formation and dissolution between neighbouring bins.
}
