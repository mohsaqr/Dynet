% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{.plot_layers}
\alias{.plot_layers}
\title{Draw the time-sliced network as a multilayer stack}
\usage{
.plot_layers(x, step, omega, palette, labels, ...)
}
\arguments{
\item{x}{A \code{dynet} object.}

\item{step}{Width of each slice, or \code{NULL} for the construction interval.}

\item{omega}{Interlayer coupling weight on the identity arcs.}

\item{palette}{Palette specification, as in \code{\link[=plot.dynet]{plot.dynet()}}.}

\item{labels}{Whether to draw vertex labels.}

\item{...}{Passed to \code{cograph::plot_mlna()}.}
}
\value{
The \code{dynet} object, invisibly. Drawn to the current device.
}
\description{
Each time slice becomes a layer of one multilayer network, and a vertex
appears once per slice. \code{cograph::plot_mlna()} needs every row and column of
the supra-adjacency to carry a unique name, so the keys are
\code{slice_vertex}; the display labels are the bare vertex names, passed
separately, so no internal key reaches the page. Assembling that pairing is
the package's job, not the caller's.
}
\keyword{internal}
