% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paths.R
\name{dyn_reachability}
\alias{dyn_reachability}
\title{Reachability of every vertex}
\usage{
dyn_reachability(
  dn,
  direction = c("both", "forward", "backward"),
  at = NULL,
  sessions = c("bounded", "collapse", "separate"),
  start = NULL,
  end = NULL,
  traversal_time = 0
)
}
\arguments{
\item{dn}{A temporal network from \code{\link[=dynet]{dynet()}}.}

\item{direction}{\code{"forward"}, \code{"backward"} or \code{"both"}.}

\item{at}{Forward source-availability time or backward arrival deadline.
Defaults to the beginning or end of each observed period, respectively.
Date and date-time values use the network's time scale. It cannot be
combined with \code{start} or \code{end}.}

\item{sessions}{How to treat sessions, as in \code{\link[=dyn_centrality]{dyn_centrality()}}.}

\item{start, end}{Inclusive lower and upper traversal-time bounds. Interval
spells remain terminus-exclusive.}

\item{traversal_time}{Nonnegative duration charged for every hop, in the
network's time unit. A calendar network also accepts a scalar \code{difftime}.}
}
\value{
A \code{dynet_metric} at node level with measures \code{forward_reach} and
\code{backward_reach}, each the proportion of other vertices involved.
}
\description{
The share of the network each vertex can reach along time-respecting paths,
and the share that can reach it. Reachability is the temporal replacement
for component membership: in a static network two vertices in the same
component reach each other by definition, whereas in a temporal network
reach depends on whether the timing lines up.
}
\details{
Reachability uses \code{\link[=dyn_paths]{dyn_paths()}} traversal semantics: nondecreasing times,
unlimited waiting, half-open interval spells, and a separate exact timestamp
rule for point events. Positive \code{traversal_time} requires interval occupancy
to finish within continuous pair activity and delays a point-trigger arrival.
For backward reachability, the resolved \code{end} is a common deadline and
latest-departure suprema determine whether a vertex can reach the target.
The canonical \code{start} and \code{end} bounds apply one closed traversal-time window
to both forward and backward queries.

In separate-session output, a session entirely outside a one-sided bound
contributes zero-reach rows rather than aborting the complete result. Its
missing implicit bound is clamped to the supplied bound, producing the
empty journey at that boundary and no eligible hop.
}
\examples{
dn <- dynet(school_contacts)
dyn_reachability(dn)
dyn_reachability(dn, start = 0, end = 10)
plot(dyn_reachability(dn))

}
