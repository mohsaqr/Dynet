% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/collapse.R
\name{collapse_network}
\alias{collapse_network}
\title{Collapse temporal activity to a static weighted network}
\usage{
collapse_network(
  dn,
  start = NULL,
  end = NULL,
  weight = c("binary", "union_duration", "total_duration", "duration_fraction",
    "spell_count", "weight_sum", "weighted_duration", "latest_weight"),
  sessions = c("bounded", "collapse", "separate"),
  censored = c("include", "exclude")
)
}
\arguments{
\item{dn}{A temporal network from \code{\link[=dynet]{dynet()}} or \code{\link[=as_dynet]{as_dynet()}}.}

\item{start, end}{Collapse bounds. Defaults to the observed range. Positive
intervals are clipped to \verb{[start, end)}; genuine points at either bound
are retained.}

\item{weight}{Edge field used as the cograph weight: \code{"binary"},
\code{"union_duration"}, \code{"total_duration"}, \code{"duration_fraction"},
\code{"spell_count"}, \code{"weight_sum"}, \code{"weighted_duration"}, or
\code{"latest_weight"}.}

\item{sessions}{Session handling. \code{"collapse"} erases session labels,
\code{"bounded"} respects session-specific endpoint activity before pooling,
and \code{"separate"} returns one collapsed cograph network per session.}

\item{censored}{Whether raw edge and vertex identities carrying an explicit
censor flag are included.}
}
\value{
A \code{dynet_collapsed} cograph netobject. With
\code{sessions = "separate"}, a named \code{dynet_collapsed_list} of such objects.
Edge data include \code{activity.duration} and \code{activity.count} aliases for
compatibility with \code{networkDynamic::network.collapse()}.
}
\description{
Creates a static cograph network from the exact observed, endpoint-valid
activity in a requested time range. Every collapsed edge retains all common
duration summaries, so choosing one weighting does not discard the others.
}
\examples{
dn <- dynet(data.frame(
  from = c("A", "A"), to = c("B", "B"),
  start = c(0, 1), end = c(2, 3)
))
flat <- collapse_network(dn, weight = "union_duration")
as.data.frame(flat)
}
