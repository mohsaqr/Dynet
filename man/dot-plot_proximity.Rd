% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/proximity.R
\name{.plot_proximity}
\alias{.plot_proximity}
\title{Draw the proximity timeline}
\usage{
.plot_proximity(
  x,
  measure = "degree",
  phases = NULL,
  networks = TRUE,
  events = TRUE,
  labels = TRUE,
  default_dist = 2,
  slices = 120L,
  window = NULL,
  flow = 2L,
  palette = "okabe",
  highlight = NULL,
  style = .dyn_style(),
  ...
)
}
\arguments{
\item{x}{A \code{dynet} object.}

\item{measure}{Node-level measure driving line thickness.}

\item{phases}{Number of phases, or \code{NULL}.}

\item{networks}{Whether to draw a network panel per phase.}

\item{events}{Whether to mark times when edges formed.}

\item{labels}{Whether to name each line at its right-hand end.}

\item{default_dist}{Distance assumed between unreachable vertices.}

\item{slices}{How many times to measure the network across the window.
Smoothness comes from measuring often, never from interpolation: nothing
is drawn between two measurements. \code{NULL} measures once per time bin.}

\item{window}{Width of each slice. \code{NULL} uses \code{\link[=.default_window]{.default_window()}}.
When slices are closer together than they are wide the windows overlap,
which is what makes consecutive positions move gradually.}

\item{flow}{Corner-cutting passes applied to each line, or \code{0} for sharp
joints. Rounding only ever takes convex combinations of measurements, so
it cannot overshoot one.}

\item{palette}{Palette specification, as in \code{\link[=plot.dynet]{plot.dynet()}}.}

\item{highlight}{Vertex names to draw in colour, the rest in grey.}

\item{style}{A style list from \code{\link[=.dyn_style]{.dyn_style()}}.}

\item{...}{Passed to \code{cograph::splot()} for the network panels.}
}
\value{
\code{x}, invisibly.
}
\description{
Draw the proximity timeline
}
\keyword{internal}
