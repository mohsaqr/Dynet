% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paths.R
\name{.path_forward_entry_open}
\alias{.path_forward_entry_open}
\title{Earliest feasible entry from a possibly open ready time}
\usage{
.path_forward_entry_open(domains, ready, ready_attained)
}
\arguments{
\item{domains}{One atom's feasible entry domains.}

\item{ready}{Earliest permitted entry.}

\item{ready_attained}{Whether entry exactly at \code{ready} is permitted.}
}
\value{
Named numeric \code{value} (\code{NA_real_} when no entry exists) and
logical-as-numeric \code{attained}.
}
\description{
\code{ready_attained = FALSE} means the traveller is ready arbitrarily close
before \code{ready} but not at it: the from-below convention the backward search
uses for suprema, applied to a forward infimum. Entry then happens just
before \code{ready} inside any domain that contains it, or exactly at the onset
of a later domain, whichever is earlier.
}
\keyword{internal}
