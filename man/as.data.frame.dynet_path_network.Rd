% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/path-visual.R
\name{as.data.frame.dynet_path_network}
\alias{as.data.frame.dynet_path_network}
\title{Tidy tables from a temporal path-union network}
\usage{
\method{as.data.frame}{dynet_path_network}(
  x,
  row.names = NULL,
  optional = FALSE,
  what = c("edges", "nodes"),
  ...
)
}
\arguments{
\item{x}{A network returned by \code{\link[=path_network]{path_network()}}.}

\item{row.names, optional}{Ignored.}

\item{what}{\code{"edges"}, the default, or \code{"nodes"}.}

\item{...}{Ignored.}
}
\value{
A plain \code{data.frame}. For \code{"edges"}, one row per hop used by an
optimal route, with \code{from}, \code{to}, \code{weight}, \code{first_time}, \code{last_time} and
\code{n_endpoints}. For \code{"nodes"}, one row per reached vertex, with \code{name},
\code{arrival_time}, \code{latency}, \code{n_hops}, \code{n_paths} and \code{groups}. See
\code{\link[=path_network]{path_network()}} for what each column means.
}
\description{
Tidy tables from a temporal path-union network
}
\examples{
dn <- dynet(school_contacts)
routes <- paths(dn, from = "Ana")
union_network <- path_network(routes)
as.data.frame(union_network, what = "edges")
as.data.frame(union_network, what = "nodes")
}
