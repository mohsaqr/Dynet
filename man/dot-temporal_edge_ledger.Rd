% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{.temporal_edge_ledger}
\alias{.temporal_edge_ledger}
\title{Integrate temporal edge opportunity, occupancy, and raw onsets}
\usage{
.temporal_edge_ledger(
  dn,
  enc,
  bin,
  sessions = c("bounded", "collapse", "separate"),
  label = "all",
  cohort = NULL
)
}
\arguments{
\item{dn}{Parent temporal network.}

\item{enc}{Encoded session block.}

\item{bin}{One reporting-window row with \code{lo}, \code{hi}, and \code{closed}.}

\item{sessions}{Session aggregation policy.}

\item{label}{Session label for a separate block.}

\item{cohort}{Optional precomputed ever-observed pair keys.}
}
\value{
Named \code{risk}, \code{occupied}, \code{observed_risk}, and \code{onsets} values.
}
\description{
Integrate temporal edge opportunity, occupancy, and raw onsets
}
\examples{
dn <- dynet(data.frame(from = "A", to = "B", start = 0, end = 2))
Dynet:::.temporal_edge_ledger(
  dn, Dynet:::.encode(dn), data.frame(lo = 0, hi = 2, closed = TRUE),
  "collapse"
)
}
\keyword{internal}
