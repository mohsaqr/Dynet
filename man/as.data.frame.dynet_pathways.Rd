% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pathways.R
\name{as.data.frame.dynet_pathways}
\alias{as.data.frame.dynet_pathways}
\title{Tidy data frame of ranked pathways}
\usage{
\method{as.data.frame}{dynet_pathways}(
  x,
  row.names = NULL,
  optional = FALSE,
  what = c("routes", "steps"),
  ...
)
}
\arguments{
\item{x}{A \code{dynet_pathways} result.}

\item{row.names}{Ignored; present for compatibility with the generic.}

\item{optional}{Ignored; present for compatibility with the generic.}

\item{what}{\code{"routes"}, the default, gives one row per distinct route.
\code{"steps"} gives one row per vertex visited -- \code{route}, \code{step}, \code{vertex}
and the \code{time} the route reaches it, preceded by \code{from} when pooled --
which is the per-hop timing the plot draws and the shape to use for any
waiting-time analysis of your own.}

\item{...}{Ignored.}
}
\value{
A plain \code{data.frame} with the columns described in \code{\link[=pathways]{pathways()}},
most frequent first, or the per-step table when \code{what = "steps"}.
}
\description{
Tidy data frame of ranked pathways
}
\examples{
as.data.frame(pathways(dynet(school_contacts), from = "Ana"))
as.data.frame(pathways(dynet(school_contacts), from = "Ana"),
              what = "steps")
}
