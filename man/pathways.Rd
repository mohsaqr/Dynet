% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pathways.R
\name{pathways}
\alias{pathways}
\title{Most frequent time-respecting routes}
\usage{
pathways(dn, from = NULL, top = NULL, min_hops = 1L, ..., plot = FALSE)
}
\arguments{
\item{dn}{A temporal network from \code{\link[=dynet]{dynet()}}.}

\item{from}{Optional source vertex. A name gives the routes leaving that
vertex, and several names give the routes leaving each of them. The
default, \code{NULL}, pools every vertex, which is the network-wide question,
and is the only case that adds a \code{from} column naming each route's source;
a named source is already the first step of every \code{route} string.}

\item{top}{Optional number of routes to keep, most frequent first. The
default keeps all of them.}

\item{min_hops}{Shortest route to report. Defaults to one, which drops the
zero-hop route from a vertex to itself.}

\item{...}{Passed to \code{\link[=paths]{paths()}}, so \code{start}, \code{end}, \code{at}, \code{direction},
\code{sessions} and \code{traversal_time} all apply.}

\item{plot}{Whether to draw the result as well as return it. Drawing is a
side effect in the manner of \code{\link[graphics:hist]{graphics::hist()}}: the verb still returns
its tidy table, invisibly when it has drawn, so \code{plot = TRUE} saves the
wrapping \code{plot()} call without changing what comes back. Use \code{plot()} on
the result when the figure needs arguments of its own.}
}
\value{
An object of class \code{dynet_pathways}, a data frame with one row per
distinct route, most frequent first: \code{route}, the vertex sequence joined
by arrows; \code{endpoint}, where it lands; \code{count}, how many optimal routes
follow it; \code{share}, its fraction of every counted route, so the shares of
a result limited by \code{top} do not sum to one; \code{n_hops}; and
\code{arrival_time}, the earliest time the route lands. Pooling over every
source adds \code{from} as the first column. Use \code{as.data.frame()} for a plain
frame and \code{as.data.frame(x, what = "steps")} for the per-hop timing of
the routes that were kept.

An unknown \code{from} raises \code{dynet_unknown_node}; a \code{top} or \code{min_hops} that
is not one finite number in range raises \code{dynet_bad_input}; and a query
that leaves no route of at least \code{min_hops} hops raises
\code{dynet_empty_result}. Conditions raised by \code{\link[=paths]{paths()}} on the arguments
passed through \code{...} reach the caller unchanged.
}
\description{
\code{pathways()} reports whole journeys rather than per-vertex summaries: one
row per distinct route, ranked by how many optimal routes follow it. It
answers "which pathways does this network actually use", where
\code{\link[=path_trajectories]{path_trajectories()}} answers "where do the routes diverge" and \code{\link[=paths]{paths()}}
answers "who is reachable".
}
\details{
The result is already ordered and already carries the share of the total,
so a caller never sorts or subsets it; \code{top} limits it in the call.

Routes are keyed on their \strong{vertex sequence}. The trajectory tree keys a
node on vertex \emph{and} time, so one sequence realised through different
contacts appears there as several branches; those are one pathway here and
their counts are added. Under the foremost criterion this loses nothing:
only earliest-arrival routes survive to be counted, so duplicates of a
sequence necessarily share an arrival time, and a test asserts it.
}
\examples{
dn <- dynet(school_contacts)
pathways(dn, from = "Ana")
pathways(dn, top = 5)

}
\seealso{
\code{\link[=paths]{paths()}} for reachability, \code{\link[=path_trajectories]{path_trajectories()}} for the prefix
tree those routes share.
}
