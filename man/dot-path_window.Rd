% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paths.R
\name{.path_window}
\alias{.path_window}
\title{Resolve a path traversal window}
\usage{
.path_window(
  dn,
  direction,
  at = NULL,
  start = NULL,
  end = NULL,
  default_start = dn$meta$time_range[["start"]],
  default_end = dn$meta$time_range[["end"]],
  clamp_missing = FALSE
)
}
\arguments{
\item{dn}{A \code{dynet} object.}

\item{direction}{\code{"forward"} or \code{"backward"}.}

\item{at}{Directional compatibility alias for an anchor.}

\item{start, end}{Canonical traversal bounds.}

\item{default_start, default_end}{Default observed bounds for this encoding.}

\item{clamp_missing}{Whether an implicit bound may clamp to an explicit one
for a non-overlapping separate session.}
}
\value{
A list containing numeric \code{start} and \code{end}.
}
\description{
Resolve a path traversal window
}
\examples{
dn <- dynet(school_contacts)
Dynet:::.path_window(dn, "forward", start = 0, end = 10)
}
\keyword{internal}
