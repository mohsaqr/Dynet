% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paths.R
\name{.optimal_path_search}
\alias{.optimal_path_search}
\title{Build the state DAG for shortest-foremost temporal paths}
\usage{
.optimal_path_search(
  enc,
  source,
  origin,
  direction = c("forward", "backward"),
  lower = -Inf,
  upper = Inf,
  traversal_time = 0,
  criterion = "foremost_then_shortest"
)
}
\arguments{
\item{enc}{Encoded edge list.}

\item{source}{Integer source (forward) or target (backward).}

\item{origin}{Source-ready time or target deadline.}

\item{direction}{Search direction.}

\item{lower, upper}{Inclusive query bounds.}

\item{traversal_time}{Nonnegative duration charged per hop.}
}
\value{
An internal optimal-path search object.
}
\description{
States are exact vertex appearances. Only minimum-hop prefixes at the same
appearance are retained, while every distinct appearance time remains.
Contact-labelled predecessor arcs preserve recurrent-contact multiplicity.
Backward states additionally carry attainment because an unattained suffix
can become attained when an incoming contact caps it strictly below its
supremum.
}
\examples{
dn <- dynet(school_contacts)
Dynet:::.optimal_path_search(
  Dynet:::.encode(dn), 1L, 0, "forward", upper = 10
)
}
\keyword{internal}
