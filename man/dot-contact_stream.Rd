% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/centrality.R
\name{.contact_stream}
\alias{.contact_stream}
\title{The eligible contact stream of a temporal network, in canonical order}
\usage{
.contact_stream(enc, dn, lower, upper)
}
\arguments{
\item{enc}{An encoding from \code{\link[=.encode]{.encode()}}.}

\item{dn}{The temporal network, for the observation test.}

\item{lower, upper}{The measurement window.}
}
\value{
A list with integer \code{from} and \code{to} and numeric \code{when}, all of the
same length, or all empty when nothing is eligible.
}
\description{
Both stream measures read the same slice of the network: uncensored contacts
whose onset falls inside the observation and inside the measurement window.
The order is fixed by \verb{(time, from, to)} so a row permutation of the input
cannot change any answer; the batch rule each measure applies then makes the
within-instant order irrelevant as well.
}
\keyword{internal}
