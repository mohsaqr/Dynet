% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/events.R
\name{dyn_events}
\alias{dyn_events}
\title{Edge formation and dissolution over time}
\usage{
dyn_events(
  dn,
  measure = c("formation", "dissolution"),
  sessions = c("bounded", "collapse", "separate")
)
}
\arguments{
\item{dn}{A temporal network from \code{\link[=dynet]{dynet()}}.}

\item{measure}{One or more of \code{"formation"} (spells beginning in the bin),
\code{"dissolution"} (spells ending in the bin), \code{"active"} (spells alive
during the bin) and \code{"new_pairs"} (vertex pairs meeting for the first
time).}

\item{sessions}{How to treat sessions, as in \code{\link[=dyn_centrality]{dyn_centrality()}}.}
}
\value{
A \code{dynet_metric} at graph level, one row per time point and
measure.
}
\description{
When relationships are born and when they die. In a static network every
edge is present at once; here the turnover itself is the finding. A course
typically shows formation front-loaded and dissolution piling up at the
end, and a group that never dissolves an edge is behaving differently from
one that constantly re-forms them.
}
\examples{
dn <- dynet(school_contacts)
dyn_events(dn)
plot(dyn_events(dn, measure = c("formation", "dissolution")))

}
