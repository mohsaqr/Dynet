% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/collapse.R
\name{as.data.frame.dynet_collapsed_list}
\alias{as.data.frame.dynet_collapsed_list}
\title{Tidy data frame of session-specific collapsed networks}
\usage{
\method{as.data.frame}{dynet_collapsed_list}(x, row.names = NULL, optional = FALSE, session = NULL, ...)
}
\arguments{
\item{x}{A \code{dynet_collapsed_list} from
\code{collapse_network(sessions = "separate")}.}

\item{row.names}{Ignored; present for compatibility with the generic.}

\item{optional}{Ignored; present for compatibility with the generic.}

\item{session}{Optional session name. Supply one to get that session's
table alone, without the \code{session} key; the default stacks them all.}

\item{...}{Ignored.}
}
\value{
A plain \code{data.frame}, one row per collapsed pair per session, with
\code{session} first and then the columns
\code{as.data.frame.dynet_collapsed()} returns.
}
\description{
Stacks the per-session edge tables into one tidy frame with a \code{session}
key, so a session-separated collapse reads like every other verb's result.
Without this the base method flattened the list sideways into a single row
of \code{s1.from}, \code{s2.from}, ... columns.
}
\examples{
dn <- dynet(data.frame(
  from = c("A", "A"), to = c("B", "B"), start = c(0, 0), end = c(2, 3),
  session = c("s1", "s2")
), session = "session")
as.data.frame(collapse_network(dn, sessions = "separate"))
as.data.frame(collapse_network(dn, sessions = "separate"), session = "s1")
}
