% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ties.R
\name{remove_arcs}
\alias{remove_arcs}
\title{Remove directed temporal arcs}
\usage{
remove_arcs(
  dn,
  ties = NULL,
  from = NULL,
  to = NULL,
  start = NULL,
  end = NULL,
  session = NULL
)
}
\arguments{
\item{dn}{A temporal network from \code{\link[=dynet]{dynet()}}.}

\item{ties}{Which ties to remove: a condition on the spell table,
evaluated the way \code{\link[=subset]{subset()}} evaluates one -- \code{duration > 2},
\code{course == "g1"} -- over the columns \code{as.data.frame(dn)} returns, tie
attributes included; or integer positions or a logical mask over that
table.}

\item{from, to, start, end, session}{Optional selectors combined by conjunction.
When \code{ties} is supplied, these selectors must be omitted. On undirected
networks \code{from} and \code{to} must be supplied together and their order is
ignored.}
}
\value{
A new directed \code{dynet} object without the matched spells, with the
same structure as the input.
}
\description{
The same operation as \code{\link[=remove_ties]{remove_ties()}}, with one extra guarantee: the
network must already be directed, so a \code{from}/\code{to} pair names one arc and
not both orientations. Removing an arc from an undirected network raises a
condition of class \code{dynet_needs_directed}.
}
\examples{
dn <- dynet(data.frame(
  from = c("A", "B"), to = c("B", "C"),
  start = c(0, 1), end = c(1, 2)
))
remove_arcs(dn, ties = 1)
}
\seealso{
\code{\link[=remove_ties]{remove_ties()}}, which does not require a directed network.
}
