% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine.R
\name{.active}
\alias{.active}
\title{Which edges are active in a given window}
\usage{
.active(enc, lo, hi, last = FALSE, window = NULL)
}
\arguments{
\item{enc}{Encoded edge list from \code{\link[=.encode]{.encode()}}.}

\item{lo, hi}{Window boundaries.}

\item{last}{Whether the window is closed on the right.}

\item{window}{The window width, which selects the rule.}
}
\value{
A logical vector, one element per edge.
}
\description{
A spell counts when it overlaps the window at all, which loses no event.
A zero-width window (\code{spec$window == 0}) instead samples the network at the
window's time point, matching \code{tsna::tSnaStats(aggregate.dur = 0)}; an edge
that starts and ends between two sample points is invisible to it.
}
\details{
Instantaneous events -- contact logs, and the final post of a thread --
belong to the window that contains them under either rule.
}
\keyword{internal}
