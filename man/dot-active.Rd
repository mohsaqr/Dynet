% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine.R
\name{.active}
\alias{.active}
\title{Which edges are active in a given bin}
\usage{
.active(enc, lo, hi, last = FALSE, sample = "window")
}
\arguments{
\item{enc}{Encoded edge list from \code{\link[=.encode]{.encode()}}.}

\item{lo, hi}{Bin boundaries.}

\item{last}{Whether this is the final bin, which is closed on the right.}

\item{sample}{Either \code{"window"} or \code{"instant"}.}
}
\value{
A logical vector, one element per edge.
}
\description{
Two sampling conventions are supported. \code{"window"} counts an edge whose
spell overlaps the bin at all, which loses no event. \code{"instant"} samples
the network at the bin's left edge, matching the point-in-time convention
used by \code{tsna::tSnaStats()} with \code{aggregate.dur = 0}; an edge that starts
and ends between two sample points is invisible to it.
}
\details{
Instantaneous events (contact logs, and the final post of a thread) belong
to the bin that contains them under either convention.
}
\keyword{internal}
