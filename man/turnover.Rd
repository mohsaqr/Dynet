% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/turnover.R
\name{turnover}
\alias{turnover}
\title{Network turnover: volatility and fluctuability}
\usage{
turnover(
  dn,
  measure = c("volatility", "fluctuability"),
  scope = c("pertime", "overall"),
  sessions = c("bounded", "collapse", "separate"),
  start = NULL,
  end = NULL,
  step = NULL,
  window = NULL,
  plot = FALSE
)
}
\arguments{
\item{dn}{A temporal network from \code{\link[=dynet]{dynet()}}.}

\item{measure}{\code{"volatility"}, the default, or \code{"fluctuability"}. Both may
be asked for at once at \code{scope = "overall"}.}

\item{scope}{\code{"pertime"}, the default, for one row per transition, or
\code{"overall"} for one row per measure. \code{"fluctuability"} is defined only at
\code{"overall"}: it is a property of the whole observed series, and a per-bin
value would be the constant 1 dressed up as data.}

\item{sessions}{How to treat sessions, as in \code{\link[=dyn_centrality]{dyn_centrality()}}.}

\item{start, end, step, window}{The measurement grid, as in \code{\link[=snapshots]{snapshots()}}.}

\item{plot}{Whether to draw the result as well as return it. Drawing is a
side effect in the manner of \code{\link[graphics:hist]{graphics::hist()}}: the verb still returns
its tidy table, invisibly when it has drawn.}
}
\value{
A \code{dynet_metric} at \code{level = "graph"}. Under \code{scope = "pertime"} the
columns are \code{time}, \code{measure} and \code{value}, one row per transition, where
\code{time} is the \strong{earlier} bin of the pair; the final bin opens no
transition and contributes no row. Under \code{scope = "overall"} they are
\code{measure} and \code{value}, one row per requested measure. A leading \code{session}
column is present under \code{sessions = "separate"}. \code{volatility} lies in
\verb{[0, 1]} and \code{fluctuability} in \verb{(0, 1]}, or is \code{NA_real_} when no pair is
active anywhere in the grid. Print it, \code{\link[=summary]{summary()}} it, \code{\link[=plot]{plot()}} it, or
take the plain frame with \code{\link[=as.data.frame]{as.data.frame()}}.
}
\description{
\code{"volatility"} is the proportion of eligible pairs whose tie state changed
between one time bin and the next. \code{"fluctuability"} is the number of
distinct pairs ever active divided by the total number of pair-bin
activations, so it is low when the same few pairs recur and high when
activity is spread thin.
}
\details{
Volatility is the \emph{proportion} Hamming distance over the eligible non-loop
pair domain: the strict upper triangle for an undirected network, every
ordered off-diagonal pair for a directed one. This is not the scale
\code{cograph::layer_similarity(method = "hamming")} uses, which is a raw count
over the full symmetric matrix and is therefore \code{2 * choose(n, 2)} times
larger on an undirected network.

\strong{Fluctuability is grid-dependent and comparable only across networks
measured on the same grid.} Its denominator sums per-bin active-pair
counts, so halving \code{step} roughly doubles it and roughly halves the result.
That is a property of the published definition rather than a defect; teneto's
own documentation concedes the measure is not normalised in a way that makes
comparisons across very different networks meaningful.

Weights and spell counts are ignored throughout: a pair is tied in a bin or
it is not. Loops are excluded from the domain.
}
\section{Conditions}{

Errors: \code{dynet_incompatible_scope} when \code{"fluctuability"} is asked for at
\code{scope = "pertime"}; \code{dynet_empty_result} when the grid yields fewer than two
bins, or when the network has fewer than two vertices and the pair domain is
empty; \code{dynet_no_sessions} when \code{sessions = "separate"} is asked of a network
with no session column; and \code{dynet_bad_input} when \code{dn} is not a \code{dynet}.
}

\examples{
dn <- dynet(school_contacts)
churn <- turnover(dn)
churn

summarised <- turnover(dn, measure = c("volatility", "fluctuability"),
                       scope = "overall")
summarised

}
\references{
Thompson, W. H., Brantefors, P., & Fransson, P. (2017). From static to
temporal network theory: applications to functional brain connectivity.
\emph{Network Neuroscience}, 1(2), 69-99. \doi{10.1162/NETN_a_00011}

Holme, P., & Saramaki, J. (2012). Temporal networks. \emph{Physics Reports},
519(3), 97-125. \doi{10.1016/j.physrep.2012.03.001}
}
\seealso{
\code{\link[=persistence]{persistence()}} for the node-level view of what survived, and
\code{\link[=events]{events()}} for raw formation and dissolution counts.
}
