% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ties.R
\name{add_arcs}
\alias{add_arcs}
\title{Add directed temporal arcs}
\usage{
add_arcs(dn, data, loops = FALSE)
}
\arguments{
\item{dn}{A temporal network from \code{\link[=dynet]{dynet()}}.}

\item{data}{A nonempty data frame with \code{from}, \code{to}, \code{start}, and \code{end}.
Optional columns are \code{weight}, \code{session}, \code{onset_censored}, and
\code{terminus_censored}. Endpoints must already exist in \code{dn}.}

\item{loops}{Whether added self-loops are permitted.}
}
\value{
A new directed \code{dynet} object.
}
\description{
Add directed temporal arcs
}
\examples{
dn <- dynet(data.frame(from = "A", to = "B", start = 0, end = 1))
dn <- add_nodes(dn, "C")
add_arcs(dn, data.frame(from = "B", to = "C", start = 1, end = 2))
}
