% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phases.R
\name{phases}
\alias{phases}
\title{Detect temporal phases by clustering the between-bin similarity}
\usage{
phases(
  dn,
  k = NULL,
  method = c("jaccard", "overlap", "hamming", "cosine", "pearson"),
  linkage = c("ward.D2", "average", "complete"),
  contiguous = TRUE,
  k_max = 10L,
  sessions = c("bounded", "collapse", "separate"),
  start = NULL,
  end = NULL,
  step = NULL,
  window = NULL
)
}
\arguments{
\item{dn}{A temporal network from \code{\link[=dynet]{dynet()}}.}

\item{k}{Number of phases. \code{NULL} chooses it by the silhouette maximum over
\code{2:k_max} and reports the whole profile, so the choice is visible and its
sensitivity checkable.}

\item{method}{How to compare two bins, passed to \code{\link[=similarity]{similarity()}}.}

\item{linkage}{Agglomeration method for \code{\link[stats:hclust]{stats::hclust()}}, used only when
\code{contiguous = FALSE}.}

\item{contiguous}{\code{TRUE}, the default and the temporally meaningful choice,
forces each phase to be an unbroken stretch of time and solves that
partition exactly. \code{FALSE} lets a phase recur, which is what cyclic data
such as weekday-and-weekend needs.}

\item{k_max}{Largest number of phases to consider when \code{k} is \code{NULL}.}

\item{sessions}{How to treat sessions.}

\item{start, end}{First and last bin times.}

\item{step}{Spacing between bin starts.}

\item{window}{Width represented by each bin.}
}
\value{
A \code{dynet_phases} frame, one row per time bin: \code{session} (only when
the network has sessions), \code{time}, \code{phase}, \code{boundary} (is this the first
bin of its phase) and \code{silhouette} (this bin's width under the chosen
\code{k}). Print it, \code{\link[=plot]{plot()}} it, or take the plain frame with
\code{\link[=as.data.frame]{as.data.frame()}}, which also serves \code{what = "profile"} for the
\eqn{k}-by-\eqn{k} sensitivity table and \code{what = "phases"} for one row
per phase.
}
\description{
A similarity heatmap invites the question "so how many regimes are there,
and where does each begin". This answers it: cluster the bins by how alike
their networks are, and report the blocks.

It is the other way of thinking about temporal structure from
\code{\link[=temporal_communities]{temporal_communities()}}. That one asks which vertices group together and
lets the grouping change over time; this one asks which \emph{moments} group
together and does not look at vertices at all.
}
\details{
The distance between two bins is \code{1 - similarity} for every method except
\code{"hamming"}, which already reports disagreement and is instead divided by
the number of vertex pairs that could carry a tie. Getting that one
backwards would silently invert the answer, so it is handled explicitly.
\code{"pearson"} similarity can be negative, so its distance runs in
\eqn{[0, 2]} rather than \eqn{[0, 1]}; that is a valid distance, but do not
expect the unit scale.

With \code{contiguous = TRUE} the bins are partitioned by Fisher's (1958)
dynamic program, which minimises total within-phase scatter over all
contiguous partitions. That is exact and deterministic. Cutting a
dendrogram is neither: it is a greedy agglomeration and it can put bin 3
and bin 40 in a phase that bin 20 is not in, which is not a regime.

\strong{Nothing here is random.} \code{\link[=temporal_communities]{temporal_communities()}} needs seeds because
modularity maximisation is a heuristic on a near-degenerate landscape; this
verb needs none because both of its steps are exact. Two calls return
identical results and the caller's random stream is never touched. The
difference is deliberate.

For the Bayesian changepoint approach to the same question -- a different
paradigm, with MCMC and its own diagnostics -- see the \code{NetworkChange}
package, which this deliberately does not reimplement.
}
\examples{
dn <- dynet(school_contacts)
phases(dn, step = 2, window = 2)
as.data.frame(phases(dn, step = 2, window = 2), what = "profile")

}
\references{
Fisher, W. D. (1958). On grouping for maximum homogeneity. \emph{Journal of the
American Statistical Association}, 53(284), 789-798.

Rousseeuw, P. J. (1987). Silhouettes: a graphical aid to the interpretation
and validation of cluster analysis. \emph{Journal of Computational and Applied
Mathematics}, 20, 53-65.

Murtagh, F., & Legendre, P. (2014). Ward's hierarchical agglomerative
clustering method: which algorithms implement Ward's criterion? \emph{Journal of
Classification}, 31, 274-295.

Lucas, M., Morris, A., Townsend-Teague, A., Tichit, L., Habermann, B. H., &
Barrat, A. (2023). Inferring cell cycle phases from a partially temporal
network of protein interactions. \emph{Cell Reports Methods}, 3(3), 100397.

Park, J. H., & Sohn, Y. (2020). Detecting structural changes in
longitudinal network data. \emph{Bayesian Analysis}, 15(1), 133-157.
}
\seealso{
\code{\link[=similarity]{similarity()}}, whose matrix this clusters, and
\code{\link[=temporal_communities]{temporal_communities()}} for the vertex-side question.
}
