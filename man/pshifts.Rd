% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pshifts.R
\name{pshifts}
\alias{pshifts}
\title{Gibson participation shifts from raw temporal turns}
\usage{
pshifts(
  dn,
  sessions = c("bounded", "collapse", "separate"),
  output = c("final", "cumulative"),
  start = NULL,
  end = NULL,
  group_events = c("simultaneous", "none")
)
}
\arguments{
\item{dn}{A directed temporal network from \code{\link[=dynet]{dynet()}}.}

\item{sessions}{Session aggregation policy.}

\item{output}{Return final class totals or cumulative rows.}

\item{start, end}{Optional inclusive query limits; each query is a fresh
sequence and never uses a predecessor outside the range.}

\item{group_events}{Infer one group-directed turn from simultaneous distinct
recipients, or retain every dyadic row.}
}
\value{
A \code{dynet_pshifts} data frame with the thirteen fixed classes,
carrying columns \code{shift}, \code{family}, \code{measure} and \code{value} — the same
\code{measure}/\code{value} pair every other measurement verb returns, so a
participation-shift census composes with the verbs that consume one.
\code{measure} is the constant \code{"count"}; \code{value} is the integer count.
\code{output = "cumulative"} prepends the turn columns; \code{sessions = "separate"} prepends \code{session}.
}
\description{
Gibson participation shifts from raw temporal turns
}
\details{
Only uncensored raw spell onsets inside the observed query and
observation components are turns; duration, weights, fragments and
terminus censoring are ignored. Consecutive turns are classified using
Gibson's fixed thirteen labels. Session and component walls, loops, ties,
duplicate multiplicity, and simultaneous-recipient group inference are
retained in metadata. \code{output = "final"} emits one typed row per class;
\code{output = "cumulative"} emits the running class vector for each turn.
}
\examples{
dn <- dynet(data.frame(
  from = c("A", "B"), to = c("B", "A"), start = c(1, 2), end = c(1, 2)
))
pshifts(dn)
}
\references{
Gibson, D. R. (2003). Participation shifts and institutional
change in relational systems. \emph{Social Forces}, 81, 1335--1380.
\doi{10.1353/sof.2003.0055}
}
