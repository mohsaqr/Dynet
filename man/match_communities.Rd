% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/communities.R
\name{match_communities}
\alias{match_communities}
\title{Give community labels a meaning that carries across time}
\usage{
match_communities(
  x,
  method = c("hungarian", "greedy"),
  overlap = c("jaccard", "intersection"),
  threshold = 0.1
)
}
\arguments{
\item{x}{A \code{dynet_communities} frame from \code{\link[=temporal_communities]{temporal_communities()}}, or any
data frame with \code{time}, \code{node} and \code{community} columns.}

\item{method}{\code{"hungarian"} solves the assignment optimally, so the answer
does not depend on the order of the rows. \code{"greedy"} takes the best
overlap first and is offered only for comparability with the published
event-detection literature; it is order-dependent and therefore not
reproducible across row permutations.}

\item{overlap}{How to score a candidate pairing: \code{"jaccard"}, the shared
members over the members of either, which is comparable across
communities of different sizes; or \code{"intersection"}, the raw count of
shared members.}

\item{threshold}{Minimum overlap for a community to inherit an earlier
label rather than start a new one. In \eqn{[0, 1]} for \code{"jaccard"}, a
non-negative count for \code{"intersection"}.}
}
\value{
A \code{dynet_communities} frame with the same rows as \code{x} and three
guarantees: \code{community} now carries the matched, time-consistent label;
\code{community_raw} keeps the original per-bin label so the matching is
auditable; and \code{event} records what happened to this state's community at
this bin, one of \code{"born"}, \code{"persist"}, \code{"split"} or \code{"merge"}. The
community-level lifecycle table, which also carries \code{"dissolve"}, is
\code{as.data.frame(x, what = "events")}.
}
\description{
A community label is arbitrary within a bin. If bin 3's "community 2" is
bin 4's "community 1", then flexibility, persistence and allegiance measure
relabelling noise and nothing else. This verb walks the bins in order and
gives a community the label of whichever earlier community it most overlaps
with, so a label means the same group throughout.

\strong{You usually do not need this.} When \code{\link[=temporal_communities]{temporal_communities()}} runs with
\code{omega > 0}, the interlayer coupling \emph{is} the matching, performed inside
the objective rather than as a post-hoc heuristic, and the labels are
already consistent. Matching is for \code{omega = 0}, for per-bin detection, for
comparing runs at different \code{gamma}, and for a partition computed
elsewhere.
}
\details{
Overlaps are computed only over the vertices active in \strong{both} bins, so a
community whose members merely go quiet is not read as dissolving.

The event taxonomy is Greene, Doyle and Cunningham's (2010), with an
optimal assignment step in place of their greedy one. A bin-\eqn{s}
community that overlaps two or more bin-\eqn{s{+}1} communities above
\code{threshold} has \strong{split}: the assigned one inherits the label and the
others are born, and every state involved is marked \code{"split"}. The mirror
case, two earlier communities feeding one later one, is a \strong{merge}. When
both descriptions fit, \code{"split"} is reported.

Labels are never recycled. A community that dissolves at bin 10 and an
unrelated one born at bin 40 cannot share a label, so a long series does
not silently reconnect two different groups.
}
\examples{
dn <- dynet(school_contacts)
loose <- temporal_communities(dn, omega = 0, step = 5, window = 5,
                              seeds = 1:3)
match_communities(loose)

}
\references{
Kuhn, H. W. (1955). The Hungarian method for the assignment problem.
\emph{Naval Research Logistics Quarterly}, 2(1-2), 83-97.

Jonker, R., & Volgenant, A. (1987). A shortest augmenting path algorithm
for dense and sparse linear assignment problems. \emph{Computing}, 38, 325-340.

Greene, D., Doyle, D., & Cunningham, P. (2010). Tracking the evolution of
communities in dynamic social networks. \emph{ASONAM 2010}, 176-183.

Palla, G., Barabasi, A.-L., & Vicsek, T. (2007). Quantifying social group
evolution. \emph{Nature}, 446, 664-667.

Cazabet, R., & Rossetti, G. (2019). Challenges in community discovery on
temporal networks. In \emph{Temporal Network Theory}, Springer, 181-197.
}
