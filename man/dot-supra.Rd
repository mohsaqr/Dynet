% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/communities.R
\name{.supra}
\alias{.supra}
\title{Reassemble a projection into per-slice adjacency matrices}
\usage{
.supra(p, symmetrise = TRUE)
}
\arguments{
\item{p}{A \code{dynet_projection} from \code{\link[=projection]{projection()}}.}

\item{symmetrise}{Whether to average a directed projection with its
transpose. Multislice modularity as published is defined for symmetric
slices; the result records that this happened rather than doing it
silently.}
}
\value{
A list with \code{nodes} (character), \code{omega}, \code{coupling},
\code{symmetrised} (logical), and \code{blocks}: one element per session block,
each a list of \code{session}, \code{times} (numeric \code{T}), \code{layers} (a list of \code{T}
\verb{n x n} matrices) and \code{active} (an \verb{n x T} logical matrix).
}
\description{
The multislice quality function needs slice-local strengths and slice-local
totals, so it needs the slices apart, not the block matrix. Materialising
the \verb{nT x nT} supra-adjacency would also be ruinous: 500 vertices over 100
bins is 50,000 states and 2.5e9 doubles, about 20 GB. Per-slice matrices
are \code{T} times \code{n^2} instead.
}
\details{
A projection of a sessioned network carries several disjoint blocks whose
identity arcs never cross a session wall, so each block gets its own slice
list and the quality function sums over blocks against one global
normaliser.
}
\examples{
dn <- dynet(school_contacts)
s <- Dynet:::.supra(projection(dn, step = 5, window = 5))
length(s$blocks[[1]]$layers)
}
\keyword{internal}
