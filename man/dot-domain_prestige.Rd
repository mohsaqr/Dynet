% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/centrality.R
\name{.domain_prestige}
\alias{.domain_prestige}
\title{Directed reachability-domain prestige}
\usage{
.domain_prestige(a, rescale = FALSE)
}
\arguments{
\item{a}{Directed adjacency matrix for one snapshot block. Positive values
are treated as binary dyads and loops have no effect.}

\item{rescale}{Whether to divide the counts by their total. A zero total
yields literal \code{NaN} for every vertex.}
}
\value{
A numeric vector of incoming nonself reachability counts, or their
block-total shares.
}
\description{
For binary directed adjacency \code{B}, let \code{H[i,j]} be true when \code{i = j} or a
directed path from \code{i} to \code{j} exists. Domain prestige excludes self and is
the incoming reachability count \code{p[j] = sum(H[,j]) - 1}. Every distinct
predecessor vertex counts once; path length and multiplicity do not enter.
}
\examples{
chain <- matrix(c(0, 1, 0, 0, 0, 1, 0, 0, 0), 3, 3, byrow = TRUE)
Dynet:::.domain_prestige(chain)
Dynet:::.domain_prestige(chain, rescale = TRUE)
}
\references{
Lin, N. (1976). \emph{Foundations of Social Research}. McGraw-Hill.

Wasserman, S., & Faust, K. (1994). \emph{Social Network Analysis: Methods and
Applications}. Cambridge University Press, Chapter 5.
}
\keyword{internal}
