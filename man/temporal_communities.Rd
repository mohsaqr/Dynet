% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/communities.R
\name{temporal_communities}
\alias{temporal_communities}
\title{Temporal community detection by generalized Louvain}
\usage{
temporal_communities(
  dn,
  gamma = 1,
  omega = 1,
  method = c("louvain", "consensus"),
  seeds = 1:10,
  coupling = c("ordinal", "categorical"),
  sessions = c("bounded", "collapse", "separate"),
  start = NULL,
  end = NULL,
  step = NULL,
  window = NULL,
  max_passes = 20L,
  tol = 1e-10
)
}
\arguments{
\item{dn}{A temporal network from \code{\link[=dynet]{dynet()}}.}

\item{gamma}{Resolution. Above one favours more, smaller communities;
below one favours fewer, larger ones.}

\item{omega}{Interlayer coupling. Zero detects each bin independently and
the labels are then matched by \code{\link[=match_communities]{match_communities()}}; large values force
one community per vertex for the whole series.}

\item{method}{\code{"louvain"} reports the best of the \code{seeds} runs.
\code{"consensus"} reports the partition the runs agree on, built from their
co-classification matrix (Lancichinetti & Fortunato 2012).}

\item{seeds}{The seeds to run from, one optimisation each. Modularity
landscapes are near-degenerate, so a single run is not a result; the
default runs ten and reports how much they agreed.}

\item{coupling}{\code{"ordinal"} couples consecutive slices, the temporal
convention. \code{"categorical"} couples every pair, which asserts that the
bins have no order.}

\item{sessions}{How to treat sessions. \code{"bounded"} and \code{"separate"} keep
session-local blocks whose coupling never crosses a session wall.}

\item{start, end}{First and last slice times. Default to observed support.}

\item{step}{Spacing between slice starts.}

\item{window}{Width represented by each slice.}

\item{max_passes}{Cap on aggregation passes per run.}

\item{tol}{Smallest improvement that counts as a move.}
}
\value{
A \code{dynet_communities} frame, one row per vertex per time bin:
\code{session} (only when the network has sessions), \code{time}, \code{node},
\code{community} (an integer label, meaning the same group in every bin),
\code{active} (was the vertex eligible in this bin) and \code{stability} (in
\eqn{[0, 1]}, how much of the run-to-run variation this state survived).
Print it, \code{\link[=summary]{summary()}} it, \code{\link[=plot]{plot()}} it, or take the plain frame with
\code{\link[=as.data.frame]{as.data.frame()}}, which also serves \code{what = "runs"}, \code{"sizes"} and,
after \code{\link[=match_communities]{match_communities()}}, \code{"events"}.
}
\description{
Which vertices form a group, and how that group survives, splits or
dissolves as time passes. The partition is found by maximising the
multislice modularity of Mucha et al. (2010) over the time-expanded
network, so the slices are solved together rather than one at a time and a
community keeps its identity across bins by construction.

Two knobs decide what you get. \code{gamma} sets the resolution: how dense a
group has to be to count as one. \code{omega} sets how much a vertex is
rewarded for staying put, so it trades a partition that tracks each bin's
structure exactly against one that persists and is readable as a story.
}
\details{
The objective is \code{\link[=multislice_modularity]{multislice_modularity()}}, and the two round-trip:
\code{multislice_modularity(dn, membership = temporal_communities(dn))} returns
the \code{q} this verb reports.

\emph{Why several seeds.} Louvain visits vertices in some order and takes the
first improving move it finds, so its answer depends on that order, and
modularity landscapes are near-degenerate: very many partitions sit within
a hair of the maximum (Good, de Montjoye & Clauset 2010). One run is a
sample from that plateau, not the answer. This verb therefore runs once per
seed, reports the best, and reports how much the runs agreed --
\code{stability_ari}, the mean pairwise adjusted Rand index across runs, and a
per-state \code{stability} column. A \code{stability_ari} near one means the labels
can be read; near zero means they cannot, whatever the modularity says.
Asking for a single seed raises a \code{dynet_single_seed} warning for that
reason.

\emph{What is not implemented.} The Leiden refinement (Traag, Waltman & van Eck
2019) is not, so a community this verb reports can in principle be
internally disconnected -- the known defect of Louvain. Running several
seeds and reading \code{stability_ari} is the mitigation on offer here.

\emph{Inactive vertices.} A vertex with no activity in a bin still has its
identity arcs, because \code{\link[=projection]{projection()}} lets a vertex wait through
inactivity. It is therefore carried along by the coupling and receives a
label rather than being dropped, which would break the chain. The \code{active}
column marks those states.

\emph{Reproducibility.} The result is a deterministic function of \code{seeds}: two
calls with the same seeds return identical frames. The caller's random
stream is saved and restored, so running this verb never changes what the
next \code{sample()} produces.
}
\examples{
dn <- dynet(school_contacts)
found <- temporal_communities(dn, step = 5, window = 5, seeds = 1:5)
found
summary(found)
as.data.frame(found, what = "sizes")

}
\references{
Mucha, P. J., Richardson, T., Macon, K., Porter, M. A., & Onnela, J.-P.
(2010). Community structure in time-dependent, multiscale, and multiplex
networks. \emph{Science}, 328(5980), 876-878.

Jeub, L. G. S., Bazzi, M., Jutla, I. S., & Mucha, P. J. (2011-2019). \emph{A
generalized Louvain method for community detection implemented in MATLAB.}

Blondel, V. D., Guillaume, J.-L., Lambiotte, R., & Lefebvre, E. (2008).
Fast unfolding of communities in large networks. \emph{Journal of Statistical
Mechanics}, P10008.

Good, B. H., de Montjoye, Y.-A., & Clauset, A. (2010). Performance of
modularity maximization in practical contexts. \emph{Physical Review E}, 81(4),
046106.

Lancichinetti, A., & Fortunato, S. (2012). Consensus clustering in complex
networks. \emph{Scientific Reports}, 2, 336.

Bassett, D. S., Porter, M. A., Wymbs, N. F., Grafton, S. T., Carlson, J.
M., & Mucha, P. J. (2013). Robust detection of dynamic community structure
in networks. \emph{Chaos}, 23(1), 013142.

Traag, V. A., Waltman, L., & van Eck, N. J. (2019). From Louvain to Leiden:
guaranteeing well-connected communities. \emph{Scientific Reports}, 9, 5233.
}
\seealso{
\code{\link[=multislice_modularity]{multislice_modularity()}} for the objective, \code{\link[=match_communities]{match_communities()}}
for labels from an uncoupled run, \code{\link[=community_change]{community_change()}} for how much the
partition moved, \code{\link[=community_trajectory]{community_trajectory()}} for what each vertex did, and
\code{\link[=phases]{phases()}} for regimes found without communities at all.
}
