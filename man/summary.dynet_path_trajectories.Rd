% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/path-trajectory.R
\name{summary.dynet_path_trajectories}
\alias{summary.dynet_path_trajectories}
\title{Summarise path trajectories}
\usage{
\method{summary}{dynet_path_trajectories}(object, ...)
}
\arguments{
\item{object}{A \code{dynet_path_trajectories} result.}

\item{...}{Ignored.}
}
\value{
A plain \code{data.frame}, one row per depth: \code{depth}, the number of
distinct \code{branches} reaching it, the \code{vertices} they land on, the summed
\code{count} of routes through it, and \code{mean_branching}, the average branching
fraction of those routes. Note \code{probability} in the underlying table is
CONDITIONAL on each parent, so it is averaged rather than summed: adding
conditional fractions across siblings would not be a probability at all.
Depth zero is the queried vertex itself and has no parent, so its
\code{mean_branching} is \code{NA}.
}
\description{
Summarise path trajectories
}
\examples{
summary(path_trajectories(paths(dynet(school_contacts), from = "Ana")))
}
