% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dynet.R
\name{.as_netobject}
\alias{.as_netobject}
\title{Assemble a temporal network as a cograph netobject}
\usage{
.as_netobject(spells, nodes, directed, groups, meta)
}
\arguments{
\item{spells}{Canonical spell table with character endpoints.}

\item{nodes}{Vertex table with a \code{name} column and any attributes.}

\item{directed}{Whether the network is directed.}

\item{groups}{Name of the vertex attribute to use as the partition, or
\code{NULL}.}

\item{meta}{Metadata list.}
}
\value{
An object of class \code{c("dynet", "netobject", "cograph_network")}.
}
\description{
A \code{dynet} object is a cograph network: it carries cograph's canonical
fields (\code{nodes}, \code{edges}, \code{directed}, \code{weights}, \code{data}, \code{meta},
\code{node_groups}) describing the aggregate network, so that
\code{cograph::splot()} renders it with no conversion, plus a \code{spells} table
holding the temporal detail cograph has no notion of.
}
\details{
The aggregate \code{edges} table uses integer endpoints because that is
cograph's schema. Nothing in Dynet's public surface exposes them: the
spell table, and every measurement result, carry vertex names.
}
\keyword{internal}
