% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{.annotate_nodes}
\alias{.annotate_nodes}
\title{Widen whole-period centrality onto the vertex table}
\usage{
.annotate_nodes(dn, nodes, measure, sessions, start, end)
}
\arguments{
\item{dn}{A \code{dynet} object.}

\item{nodes}{The plain vertex table to annotate.}

\item{measure}{Character vector of measures.}

\item{sessions}{Session mode.}

\item{start, end}{Optional measurement bounds.}
}
\value{
\code{nodes} with one added column per measure, and a leading \code{session}
column when sessions are reported separately.
}
\description{
One column per measure, valued over a single window covering the observed
period, so a caller can rank or filter vertices with \code{subset()} instead of
reshaping a long measure frame by hand. \code{"indegree"} and \code{"outdegree"} are
spellings of \code{degree} under \code{mode}, which the measure vocabulary does not
otherwise expose at node level.
}
\examples{
dn <- dynet(school_contacts)
Dynet:::.annotate_nodes(dn, as.data.frame(dn, what = "nodes"),
                        "degree", "bounded", NULL, NULL)
}
\keyword{internal}
