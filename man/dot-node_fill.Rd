% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{.node_fill}
\alias{.node_fill}
\title{Vertex fill colours, following the partition when there is one
cograph's own \code{palette_colorblind()} is an interpolated ramp, not the
Okabe-Ito set, so the partition colours come from \code{\link[=.dyn_palette]{.dyn_palette()}}.}
\usage{
.node_fill(net, palette = "okabe")
}
\arguments{
\item{net}{A netobject carrying a \code{groups} column.}

\item{palette}{Palette specification, as in \code{\link[=plot.dynet]{plot.dynet()}}.}
}
\value{
A character vector of colours, one per vertex.
}
\description{
Vertex fill colours, following the partition when there is one
cograph's own \code{palette_colorblind()} is an interpolated ramp, not the
Okabe-Ito set, so the partition colours come from \code{\link[=.dyn_palette]{.dyn_palette()}}.
}
\keyword{internal}
