% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine.R
\name{.window_spec}
\alias{.window_spec}
\title{Resolve and validate the four measurement-grid arguments}
\usage{
.window_spec(dn, start = NULL, end = NULL, step = NULL, window = NULL)
}
\arguments{
\item{dn}{A \code{dynet} object.}

\item{start, end}{Measurement range, or \code{NULL} for the observed range.}

\item{step}{Spacing between measurements, or \code{NULL} for the network's
construction interval.}

\item{window}{Width of each measurement, or \code{NULL} for \code{step}.}
}
\value{
A list with \code{start}, \code{end}, \code{step} and \code{window}.
}
\description{
Called once at the top of every verb that measures over time, so the
contract is checked in one place rather than at each use.
}
\details{
\code{start} and \code{end} are left \code{NULL} when the caller did not supply them, so
that each session can fall back to its own observed range; an explicit
value applies to every session alike.
}
\keyword{internal}
