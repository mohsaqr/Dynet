% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine.R
\name{.vertex_eligibility}
\alias{.vertex_eligibility}
\title{Eligible vertices in one reporting window and session scope}
\usage{
.vertex_eligibility(
  activity,
  bin,
  window,
  session = NULL,
  erase_sessions = FALSE
)
}
\arguments{
\item{activity}{Encoded activity from \code{\link[=.encode_vertex_activity]{.encode_vertex_activity()}}.}

\item{bin}{One row from the measurement grid.}

\item{window}{Reporting-window width.}

\item{session}{Optional session label. \code{NULL} applies global rows; a label
applies global rows and matching session rows.}

\item{erase_sessions}{Whether all session labels are calendar-unioned.}
}
\value{
A fixed-order logical vector.
}
\description{
Eligible vertices in one reporting window and session scope
}
\examples{
dn <- dynet(data.frame(from = "A", to = "B", start = 0, end = 2),
            vertex_spells = data.frame(node = "A", start = 0, end = 1))
va <- Dynet:::.encode_vertex_activity(dn)
Dynet:::.vertex_eligibility(va,
  data.frame(lo = 0, hi = 0, closed = TRUE), 0)
}
\keyword{internal}
