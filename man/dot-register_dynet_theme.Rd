% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme.R
\name{.register_dynet_theme}
\alias{.register_dynet_theme}
\title{Register Dynet's cograph theme}
\usage{
.register_dynet_theme()
}
\value{
\code{TRUE} if the theme was registered, \code{FALSE} otherwise, invisibly.
}
\description{
cograph keeps a theme registry (\code{cograph::register_theme()},
\code{cograph::get_theme()}, \code{cograph::list_themes()}) and \code{cograph::splot()}
takes a \code{theme} argument. Dynet registers \code{"dynet"} there on load, so it
appears in \code{cograph::list_themes()} alongside cograph's own and can be
asked for by name from any cograph call.
}
\details{
The theme must be a \code{CographTheme} object rather than the plain list
\code{register_theme()} documents, because \code{splot()} reaches into it with
\code{th$get()}; a list is accepted by the registry and then fails at draw time.

\code{splot()} reads six of the thirteen parameters a theme can hold --
\code{node_fill}, \code{background}, \code{label_color}, \code{edge_positive_color},
\code{edge_negative_color} and \code{node_border_color}, the last of which a
\code{CographTheme} stores under the name \code{node_border} and so never matches.
Everything a theme cannot carry stays in \code{\link[=.splot_args]{.splot_args()}}.
}
\keyword{internal}
