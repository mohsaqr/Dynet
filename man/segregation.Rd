% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/segregation.R
\name{segregation}
\alias{segregation}
\title{Segregation-integration difference over time}
\usage{
segregation(
  dn,
  communities,
  scope = c("pertime", "overall"),
  sessions = c("bounded", "collapse", "separate"),
  start = NULL,
  end = NULL,
  step = NULL,
  window = NULL,
  plot = FALSE
)
}
\arguments{
\item{dn}{A temporal network from \code{\link[=dynet]{dynet()}}.}

\item{communities}{Either a vector \strong{named by vertex name}, or a single
string naming a column of the node table, as \code{\link[=mixing]{mixing()}} takes. A
positional vector is refused: matching a partition to vertices by
position is exactly the index-based addressing this package avoids.}

\item{scope}{\code{"pertime"}, the default, for one row per bin, or \code{"overall"}
for the mean over bins.}

\item{sessions}{How to treat sessions, as in \code{\link[=dyn_centrality]{dyn_centrality()}}.}

\item{start, end, step, window}{The measurement grid, as in \code{\link[=snapshots]{snapshots()}}.}

\item{plot}{Whether to draw the result as well as return it. Drawing is a
side effect in the manner of \code{\link[graphics:hist]{graphics::hist()}}: the verb still returns
its tidy table, invisibly when it has drawn.}
}
\value{
A \code{dynet_metric} at \code{level = "graph"} with columns \code{time} (under
\code{scope = "pertime"}), \code{measure}, which is the constant \code{"sid"}, and
\code{value}. A leading \code{session} column is present under
\code{sessions = "separate"}. \strong{The value is signed and unbounded}: it is a
difference of two normalised sums, not a proportion, so there is no range
to check it against. Print it, \code{\link[=summary]{summary()}} it, \code{\link[=plot]{plot()}} it, or take the
plain frame with \code{\link[=as.data.frame]{as.data.frame()}}.
}
\description{
Fransson's SID: for each time bin, how much more tied a network's
communities are internally than to each other, both normalised by the
number of pairs available. Positive means segregation exceeds integration.
}
\details{
A community of one vertex makes the within-community normaliser
\code{2 / (N_a (N_a - 1))} a division by zero. Rather than drop the vertex,
which would change every other community's denominator, the bin returns
\code{NA_real_} and a \code{dynet_singleton_community} warning names the offending
community.

The measure is defined on undirected networks, and a directed one is read
by folding each layer onto its transpose, as \code{\link[=persistence]{persistence()}} does. Weights
and spell counts are ignored: a pair is tied in a bin or it is not.
}
\section{Conditions}{

Errors: \code{dynet_bad_communities} when the partition is unnamed, names a
vertex the network does not have, or leaves a vertex unassigned;
\code{dynet_no_sessions} under \code{sessions = "separate"} without a session column;
\code{dynet_bad_input} when \code{dn} is not a \code{dynet}. Warns with
\code{dynet_singleton_community} when a community has one member.
}

\examples{
dn <- dynet(school_contacts)
groups <- stats::setNames(
  rep(c("a", "b"), length.out = nrow(dn$nodes)), dn$nodes$name
)
sid <- segregation(dn, communities = groups)
sid

}
\references{
Fransson, P., Thompson, W. H., Skiold, B., et al. (2018). Brain network
segregation and integration during an epoch-related working memory fMRI
experiment. \emph{NeuroImage}, 178, 147-161.
\doi{10.1016/j.neuroimage.2018.05.040}

Thompson, W. H., Brantefors, P., & Fransson, P. (2017). From static to
temporal network theory. \emph{Network Neuroscience}, 1(2), 69-99.
\doi{10.1162/NETN_a_00011}
}
\seealso{
\code{\link[=mixing]{mixing()}} for who ties to whom by attribute, and \code{\link[=persistence]{persistence()}}
for whether those ties survive.
}
