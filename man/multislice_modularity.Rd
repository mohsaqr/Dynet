% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/communities.R
\name{multislice_modularity}
\alias{multislice_modularity}
\title{Multislice modularity of a partition of a temporal network}
\usage{
multislice_modularity(
  dn,
  membership = NULL,
  gamma = 1,
  omega = 1,
  coupling = c("ordinal", "categorical"),
  sessions = c("bounded", "collapse", "separate"),
  start = NULL,
  end = NULL,
  step = NULL,
  window = NULL
)
}
\arguments{
\item{dn}{A temporal network from \code{\link[=dynet]{dynet()}}.}

\item{membership}{A data frame with columns \code{time}, \code{node} and \code{community},
plus \code{session} when the network has sessions. This is exactly the shape
\code{\link[=temporal_communities]{temporal_communities()}} returns. \code{NULL} scores the partition that puts
every state in one community.}

\item{gamma}{Resolution. Values above one favour more, smaller
communities; values below one favour fewer, larger ones.}

\item{omega}{Interlayer coupling: how much a vertex is rewarded for keeping
its community from one slice to the next. Zero scores the slices
independently.}

\item{coupling}{Which slices are coupled, \code{"ordinal"} (consecutive, the
temporal convention) or \code{"categorical"} (all pairs). See \code{\link[=projection]{projection()}}.}

\item{sessions}{How to treat sessions: \code{"bounded"} and \code{"separate"} keep
session-local blocks whose coupling never crosses a session wall,
\code{"collapse"} erases the labels.}

\item{start, end}{First and last slice times. Default to observed support.}

\item{step}{Spacing between slice starts.}

\item{window}{Width represented by each slice.}
}
\value{
A \code{dynet_metric} at graph level, one row per component of the
decomposition: \code{q} (the multislice modularity), \code{q_intra} and \code{q_inter}
(its within-slice and interlayer parts, which sum to \code{q}), \code{two_mu} (the
normaliser), \code{n_communities} and \code{n_empty_slices}. Columns are \code{measure}
and \code{value}; there is no \code{time} column because \eqn{Q} is a property of
the whole series, not of a bin. Attributes carry \code{gamma}, \code{omega},
\code{coupling} and \code{symmetrised}.
}
\description{
The Mucha et al. (2010) quality function: how much better a partition
explains the network than a degree-preserving null does, computed slice by
slice with the slices tied together by the interlayer coupling \code{omega}.

Score a partition you already have -- a class roster, a set of research
groups, an externally computed clustering -- or score the one
\code{\link[=temporal_communities]{temporal_communities()}} found. It is the objective that verb maximises, so
the two round-trip with no reshaping by the caller.
}
\details{
With \eqn{A_{ijs}} the weight of edge \eqn{i}--\eqn{j} in slice \eqn{s},
\eqn{k_{is}} its slice-local strength, \eqn{2m_s} the slice total,
\eqn{\omega_{jsr}} the coupling of vertex \eqn{j} between slices \eqn{s}
and \eqn{r}, and \eqn{2\mu = \sum_{js}(k_{js} + \sum_r \omega_{jsr})},
\deqn{Q = \frac{1}{2\mu}\sum_{ijsr}\left[\left(A_{ijs} -
  \gamma\frac{k_{is}k_{js}}{2m_s}\right)\delta_{sr} +
  \delta_{ij}\omega_{jsr}\right]\delta(g_{is}, g_{jr}).}

Three things in that formula are not the static null, and getting any of
them wrong gives a different objective that still looks plausible:
\enumerate{
\item The null uses \eqn{k_{is}}, \eqn{k_{js}} and \eqn{2m_s} \strong{from slice
\eqn{s} alone}, never the supra-degree. That is what lets a sparse bin
and a dense bin be compared at all.
\item The null is multiplied by \eqn{\delta_{sr}}, so \strong{no null is subtracted
from the interlayer arcs}. Identity arcs are not edges to be explained
away; they assert that a vertex is the same vertex.
\item The normaliser \eqn{2\mu} \strong{includes} the coupling strength, which is
why \eqn{Q} does not diverge as \code{omega} grows.
}

Running \code{igraph::cluster_louvain()} on the supra-adjacency matrix gets all
three wrong: it applies the static Newman--Girvan null to the whole
supra-graph. That is a different objective function, not a different
implementation of this one.

Two exact reductions follow from the formula and are pinned by tests. With
one slice and \code{omega = 0}, \eqn{Q} is ordinary Newman--Girvan modularity.
With \code{omega = 0} and any number of slices, \eqn{Q} is the \eqn{2m_s}-
weighted mean of the per-slice Newman--Girvan modularities.

An edgeless slice has \eqn{2m_s = 0} and would divide by zero; its null
contribution is defined as exactly zero, and \code{n_empty_slices} reports how
many such slices there were. A network with no edges and no coupling has
\eqn{2\mu = 0} and no defined \eqn{Q} at all, which raises
\code{dynet_empty_result} rather than returning \code{NaN}.

A directed network is averaged with its transpose before scoring, because
the published quality function is defined for symmetric slices. The result
records this in its \code{symmetrised} attribute.
}
\examples{
dn <- dynet(school_contacts)
multislice_modularity(dn, step = 5, window = 5)
multislice_modularity(dn, membership = temporal_communities(
  dn, step = 5, window = 5, seeds = 1:3
), step = 5, window = 5)

}
\references{
Mucha, P. J., Richardson, T., Macon, K., Porter, M. A., & Onnela, J.-P.
(2010). Community structure in time-dependent, multiscale, and multiplex
networks. \emph{Science}, 328(5980), 876-878.

Newman, M. E. J., & Girvan, M. (2004). Finding and evaluating community
structure in networks. \emph{Physical Review E}, 69(2), 026113.

Reichardt, J., & Bornholdt, S. (2006). Statistical mechanics of community
detection. \emph{Physical Review E}, 74(1), 016110.

Bazzi, M., Porter, M. A., Williams, S., McDonald, M., Fenn, D. J., &
Howison, S. D. (2016). Community detection in temporal multilayer networks,
with an application to correlation networks. \emph{Multiscale Modeling &
Simulation}, 14(1), 1-41.
}
\seealso{
\code{\link[=temporal_communities]{temporal_communities()}}, which maximises this quantity, and
\code{\link[=projection]{projection()}}, which builds the time-expanded network it is defined on.
}
